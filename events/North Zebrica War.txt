add_namespace = north_war

country_event = { #Hippogriffia Receives the White Peace Offer from Chiropterra/Pact
	id = north_war.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event north_war.1"}
	title = north_war.1.t
	desc = north_war.1.d

	picture = GFX_event_sao_macaw
	is_triggered_only = yes

	option = { #Accept
	    ai_chance = {
			factor = 90
		}
		hidden_effect = {
		}
	}
	
	option = { #Refuse
	    ai_chance = {
			factor = 10
		}
		name = north_war.1.b
		custom_effect_tooltip = northern_total_war_tp
		add_stability = -0.20
		add_war_support = -0.20
		if = {
		    limit = {
			    has_government = democratic
			}
			add_popularity = {
				ideology = democratic
				popularity = -0.15
			}
		}
		if = {
		    limit = {
			    has_government = neutrality
			}
			add_popularity = {
				ideology = neutrality
				popularity = -0.15
			}
		}
		if = {
		    limit = {
			    has_government = communism
			}
			add_popularity = {
				ideology = communism
				popularity = -0.15
			}
		}
		if = {
		    limit = {
			    has_government = neutrality
			}
			add_popularity = {
				ideology = neutrality
				popularity = -0.15
			}
		}
		hidden_effect = {
		}
	}
}