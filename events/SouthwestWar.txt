add_namespace = southwest

country_event = {
	id = southwest.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event southwest.1"}
	title = southwest.1.t
	desc = southwest.1.d

	is_triggered_only = yes

	option = {
		name = southwest.1.a
		if = {
			limit = {
				tag = LSP
				is_ai = no
				SCS = {
					is_ai = yes
				}
			}
			create_faction = SCS_LSP_faction
			add_to_faction = SCS
		}
		if = {
			limit = {
				tag = SCS
			}
			if = {
				limit = {
					OR = {
						LSP = {
							is_ai = yes
						}
						AND = {
							LSP = {
								is_ai = no
							}
							SCS = {
								is_ai = no
							}
						}
					}
				}
				create_faction = SCS_LSP_faction
				add_to_faction = LSP
			}
		}
	}
}

country_event = {
	id = southwest.2
	immediate = {log = "[GetDateText]: [Root.GetName]: event southwest.2"}
	title = southwest.2.t
	desc = southwest.2.d

	is_triggered_only = yes

	option = {
		name = southwest.2.a
		if = {
			limit = {
				tag = ROC
				is_ai = no
				BUF = {
					is_ai = yes
				}
			}
			create_faction = BUF_ROC_faction
			add_to_faction = BUF
		}
		if = {
			limit = {
				tag = BUF
			}
			if = {
				limit = {
					OR = {
						ROC = {
							is_ai = yes
						}
						AND = {
							ROC = {
								is_ai = no
							}
							BUF = {
								is_ai = no
							}
						}
					}
				}
				create_faction = BUF_ROC_faction
				add_to_faction = ROC
			}
		}
	}
}

country_event = {
	id = southwest.3
	immediate = {log = "[GetDateText]: [Root.GetName]: event southwest.3"}
	title = southwest.3.t
	desc = southwest.3.d

	is_triggered_only = yes

	option = {
		name = southwest.3.a
		if = {
			limit = { is_faction_leader = yes }
			dismantle_faction = yes
		}
	}
}

