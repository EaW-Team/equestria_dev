@manifest_fulfilled_value = 0.75

faction_manifest_ruling_the_rivers = {
	
	name = faction_manifest_ruling_the_rivers_name
	description = faction_manifest_ruling_the_rivers_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:riverlands_controllable_states
		completed_amount_collection = collection:riverlands_controlled_states
		
		scale = {
			trade_opinion_factor = 0.20
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					political_power_factor = 0.1
				}			
			}
		}
	}
}

faction_manifest_one_continent_under_me = {
	
	name = faction_manifest_one_continent_under_me_name
	description = faction_manifest_one_continent_under_me_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:states_in_europe
		completed_amount_collection = collection:faction_states_in_europe
		
		
		scale = {
			resistance_target = -0.1
			non_core_manpower = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					compliance_growth = 0.1
				}			
			}
		}
	}
}

faction_manifest_the_greatest_navy_in_the_world = {
	name = faction_manifest_the_greatest_navy_in_the_world_name
	description = faction_manifest_the_greatest_navy_in_the_world_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:non_capitulated_majors
		completed_amount_collection = collection:non_capitulated_majors_with_smaller_navy_than_HIP
		

		scale = {
			navy_fuel_consumption_factor = -0.25
			fuel_gain_factor = 0.25
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					production_lack_of_resource_penalty_factor = -0.2
				}			
			}
		}
	}
}

faction_manifest_unite_the_griffons = {
	name = faction_manifest_unite_the_griffons_name
	description = faction_manifest_unite_the_griffons_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:former_imperial_territories
		completed_amount_collection = collection:controlled_former_imperial_territories
		
		scale = {
			army_morale_factor = 0.05
			compliance_growth = 0.1
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					factory_energy_consumption = -0.1
				}			
			}
		}
	}
}

# Triumph of Harmony
faction_manifest_triumph_of_harmony = {
	name = faction_manifest_triumph_of_harmony_name
	description = faction_manifest_triumph_of_harmony_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:states_on_my_continent
		completed_amount_collection = collection:non_fascist_controlled_states_on_my_continent
		range = { 
			min = 0.5
		}
		scale = {
			fascism_drift = -0.08
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					weekly_casualties_war_support = 0.006
				}
			}
		}
	}
}

# Rebuilding Equestria
faction_manifest_rebuilding_equestria = {
	name = faction_manifest_rebuilding_equestria_name
	description = faction_manifest_rebuilding_equestria_desc
	is_manifest = yes

	ratio_progress = {
		total_amount_collection = collection:greater_equestrian_territories
		completed_amount_collection = collection:controlled_greater_equestrian_territories
		
		scale = {
			annex_cost_factor = -0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					stability_factor = 0.08
				}
			}
		}
	}
}

# Controlling North Zebrica
faction_manifest_controlling_north_zebrica = {
	name = faction_manifest_controlling_north_zebrica_name
	description = faction_manifest_controlling_north_zebrica_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:continental_north_zebrican_territories
		completed_amount_collection = collection:controlled_continental_north_zebrican_territories
		
		scale = {
			industrial_capacity_factory = 0.1
		}

		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					production_speed_rail_way_factor = 0.15
				}			
			}
		}
	}
}

# Controlling East Zebrica
faction_manifest_controlling_east_zebrica = {
	name = faction_manifest_controlling_east_zebrica_name
	description = faction_manifest_controlling_east_zebrica_desc
	is_manifest = yes

	ratio_progress = {
		total_amount_collection = collection:east_zebrican_territories
		completed_amount_collection = collection:controlled_east_zebrican_territories
		
		scale = {
			annex_cost_factor = -0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					stability_factor = 0.08
				}
			}
		}
	}
}

# Controlling South Zebrica
faction_manifest_controlling_south_zebrica = {
	name = faction_manifest_controlling_south_zebrica_name
	description = faction_manifest_controlling_south_zebrica_desc
	is_manifest = yes

	ratio_progress = {
		total_amount_collection = collection:south_zebrican_territories
		completed_amount_collection = collection:controlled_south_zebrican_territories
		
		scale = {
			local_resources_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					war_support_factor = 0.05
				}
			}
		}
	}
}

# Eradicating avians
faction_manifest_eradicating_avians = {
	name = faction_manifest_eradicating_avians_name
	description = faction_manifest_eradicating_avians_desc
	is_manifest = yes

	ratio_progress = {
		total_amount_collection = collection:avain_states
		completed_amount_collection = collection:controlled_avian_states
		
		scale = {
			army_morale_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					stability_factor = 0.05
				}
			}
		}
	}
}

# Spread of Communism (Continent edition)
# This is kinda adapted from "An Iron Curtain", except with non-gaga modifiers
faction_manifest_spread_of_communism_same_continent = {
	name = faction_manifest_spread_of_communism_same_continent_name
	description = faction_manifest_spread_of_communism_same_continent_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:countries_my_continent
		completed_amount_collection = collection:communist_or_capitulated_countries_my_continent
		
		scale = {
			communism_drift = 0.08
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					industrial_capacity_factory = 0.05
					industrial_capacity_dockyard = 0.05
				}
			}
		}
	}
}

# Stop Extremism
faction_manifest_stop_extremism = {
	name = faction_manifest_stop_extremism_name
	description = faction_manifest_stop_extremism_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:countries_my_continent
		completed_amount_collection = collection:extremist_controlled_states_my_continent
		

		scale = {
			fascism_drift = -0.05
			communism_drift = -0.05
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					stability_factor = 0.1
				}
			}
		}
	}
}

# Death To Supremacy (sneaky global - only for Posada)
faction_manifest_death_to_supremacy = {
	name = faction_manifest_death_to_supremacy_name
	description = faction_manifest_death_to_supremacy_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_fascist_controlled_states
		
		scale = {
			nuclear_production_factor = 0.2
			thermonuclear_production_factor = 0.2
		}
		range = { 
			min = 0.5
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					party_popularity_stability_factor = 0.05
				}
			}
		}
	}
}

# Controlling West Zebrica
faction_manifest_controlling_west_zebrica = {
	name = faction_manifest_controlling_west_zebrica_name
	description = faction_manifest_controlling_west_zebrica_desc
	is_manifest = yes

	ratio_progress = {
		total_amount_collection = collection:west_zebrican_territories
		completed_amount_collection = collection:controlled_west_zebrican_territories
		
		scale = {
			local_resources_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					conscription_factor = 0.05
				}
			}
		}
	}
}

# Global Fascist Revolution (sneaky global - only for Zephyr)
faction_manifest_victory_of_fascism = {
	name = faction_manifest_victory_of_fascism_name
	description = faction_manifest_victory_of_fascism_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_fascist_controlled_states
		
		scale = {
			weekly_bombing_war_support = 0.005
			weekly_convoys_war_support = 0.005
			weekly_casualties_war_support = 0.005
		}
		range = { 
			max = 0.5 # half the world should be enough
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					fascism_drift_from_guarantees = 0.1
					fascism_drift = 0.1
					master_ideology_drift = 0.1
				}
			}
		}
	}
}