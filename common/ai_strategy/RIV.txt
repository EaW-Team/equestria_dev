RIV_empire_is_causing_trouble = {
	enable = {
		tag = RIV
		has_war = no

		GRI = {
			has_offensive_war = yes
			NOT = { has_government = democratic }
		}
	}
	abort = {
		OR = {
			is_in_faction_with = GRI
			GRI = {
				has_government = democratic
			}			
			GRI = {
				has_war = no 
			}
		}
	}
	
	ai_strategy = {
		type = contain
		id = "GRI"			
		value = 200
	}
}