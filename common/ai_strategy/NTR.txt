NTR_SRS_border_wars = {
	reversed = yes
	
	enable = {
		tag = NTR
	}
	
	abort = {
		NOT = {
			has_idea = SRS_northern_war
		}
	}
	
	ai_strategy = {
		type = conquer
		id = "SRS"
		value = 300
	}
}

NTR_MNC_border_skirmishes = {
	reversed = yes
	
	enable = {
		tag = NTR
	}
	
	abort = {
		OR = {
			is_guaranteed_by = MNC
			has_non_aggression_pact_with = MNC
			
			MNC = {
				is_guaranteed_by = ROOT
			}
		}
	}
	
	ai_strategy = {
		type = antagonize
		id = "MNC"
		value = 150
	}
}

NTR_reintegration_pp_spend_amount = {
	enable = {
		tag = NTR
		
		NOT = { has_country_flag = NTR_reintegration_completed }
	}
	
	abort = {
		has_country_flag = NTR_reintegration_completed
	}
	
	ai_strategy = {
		type = pp_spend_amount
		id = idea
		value = -50 # 100 -> 50
	}
	
	ai_strategy = {
		type = pp_spend_amount
		id = decision
		value = 150 # 50 -> 200
	}
	
	ai_strategy = {
		type = pp_spend_priority
		id = guarantee
		value = -100 # You are serious?
	}
}

NTR_isolation = {
	reversed = yes
	
	enable = {
		tag = NTR
		
		has_war = no
		is_subject = no
		has_government = neutrality
		has_completed_focus = NTR_strengthen_the_community
		
		all_neighbor_country = {
			NOT = {
				has_or_is_justifying_wargoal_against_ROOT = yes
			}
		}
	}
	
	abort = {
		OR = {
			has_war = yes
			is_subject = yes
			
			NOT = {
				has_government = neutrality
			}
			
			any_neighbor_country = {
				has_or_is_justifying_wargoal_against_ROOT = yes
			}
		}
	}
	
	ai_strategy = {
		type = ignore
		id = "VED"
		value = 150
	}
	
	ai_strategy = {
		type = ignore
		id = "FAT"
		value = 150
	}
	
	ai_strategy = {
		type = ignore
		id = "MNC"
		value = 100
	}
	
	ai_strategy = {
		type = ignore
		id = "HLR"
		value = 50
	}
}

NTR_war_with_DED = {
	enable = {
		tag = NTR
		
		DED = {
			aggressive_towards_PREV = yes
		}
	}
	
	abort = {
		NOT = {
			country_exists = DED
		}
	}
	
	ai_strategy = {
		type = conquer
		id = "DED"
		value = 100
	}
	
	ai_strategy = {
		type = antagonize
		id = "DED"
		value = 250
	}
	
	ai_strategy = {
        type = force_build_armies
        value = 50
    }
	
	ai_strategy = {
		type = unit_ratio
		id = artillery
		value = 30
	}
}