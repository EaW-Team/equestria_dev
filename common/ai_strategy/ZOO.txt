ZOO_unify_homeland = {
	allowed = {
		tag = ZOO
	}
	enable = {
		OR = {
			has_war_with = IST
			has_war_with = IDO
			has_war_with = DEL
			has_war_with = MEL
			has_war_with = TAI
		}
		NOT = {
			has_war_with = KIR
			has_war_with = KHM
			has_war_with = KXM
			has_war_with = NVA
			has_war_with = IBZ
			has_war_with = TSV
			has_war_with = ROA
			has_war_with = GFS
			has_war_with = GIF
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_control
		area = zebrica_southeast_jungle
		ratio = 0
		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
		manual_attack = yes
	}
	ai_strategy = {
		type = garrison
		value = -100
	}
}
ZOO_push_jungle = {
	allowed = {
		original_tag = ZOO
	}
	enable = {
		OR = {
			has_war_with = IST
			has_war_with = IDO
			has_war_with = DEL
		}
		NOT = {
			has_war_with = KIR
			has_war_with = KXN
			has_war_with = KHM
			has_war_with = NVA
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_control
		area = zebrica_southeast_jungle
		ratio = 0
		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
		manual_attack = yes
	}
}
ZOO_general_strategy = {
	allowed = {
		tag = ZOO
	}
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = equipment_market_for_sale_factor
		id = infantry
		value = -50
	}
	ai_strategy = {
		type = equipment_market_for_sale_factor
		id = support
		value = -25
	}
	ai_strategy = {
		type = equipment_market_for_sale_factor
		id = artillery
		value = -25
	}
}

ZOO_slow_kirin_strategy = {
	allowed = {
		tag = ZOO
	}
	enable = {
		OR = {
			has_war_with = KXN
			has_war_with = NVA
			has_war_with = KHM
		}
		NOT = {
			has_war_with = NVA
			has_war_with = IBZ
			has_war_with = TSV
			has_war_with = ROA
			has_war_with = GFS
			has_war_with = GIF
		}
		alliance_strength_ratio < 2
		date < 1011.2.1
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			OR = {
				tag = KXN
				tag = NVA
				tag = KHM
			}
		}

		ratio = 0

		priority = 200
		ordertype = front
		execution_type = careful
	}
	ai_strategy = {
		type = garrison
		value = -100
	}
}

ZOO_protracted_war_strategy = {
	allowed = {
		tag = ZOO
	}
	enable = {
		surrender_progress > 0.6
		date < 1014.1.1
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = put_unit_buffers
		ratio = 0.15
		states = {
			1138
		}
		subtract_fronts_from_need = yes
	}
}

MQA_defend_ally = {
	allowed = {
		tag = MQA
	}
	enable = {
		has_war = yes
		any_allied_country = {
			tag = ZOO
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = force_defend_ally_borders
		id = ZOO
		value = 50
	}
	ai_strategy = {
		type = garrison
		value = -75
	}
	ai_strategy = {
		type = send_lend_lease_desire
		id = "ZOO"
		value = 500
	}
}

ancient_pact_AI_should_support_zongo = {
	allowed = {
		OR = {
			original_tag = CNT
			original_tag = GAR
		}
		
	}
	enable = {
		ZOO = {
			OR = {
				has_war_with = KXN
				has_war_with = NVA
				has_war_with = KHM
				has_war_with = ZLU
				has_war_with = GFS
				has_war_with = GIF
				has_war_with = ROA
				has_war_with = IBZ
				has_war_with = TSV
				has_war_with = KIR
			}
		}
	}

	abort_when_not_enabled = yes
	ai_strategy = {
		type = send_volunteers_desire
		id = ZOO
		value = 300
	}
	ai_strategy = {
		type = send_lend_lease_desire
		id = ZOO
		value = 300
	}
	ai_strategy = {
		type = equipment_market_trade_desire
		id = ZOO
		value = 300 
	}
}