ZOO_has_custom_railroad_mechanic = {
	allowed = {
		tag = ZOO
	}
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = build_building
		id = rail_way
	
		value = -9999
	}
}
ZOO_unify_homeland = {
	allowed = {
		tag = ZOO
	}
	enable = {
		OR = {
			has_war_with = IST
			has_war_with = IDO
			has_war_with = DEL
			has_war_with = MEL
			has_war_with = TAI
		}
		NOT = {
			has_war_with = KIR
			has_war_with = KHM
			has_war_with = KXM
			has_war_with = NVA
			has_war_with = IBZ
			has_war_with = TSV
			has_war_with = ROA
			has_war_with = GFS
			has_war_with = GIF
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_control
		area = zebrica_southeast_jungle
		ratio = 0
		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
		manual_attack = yes
	}
}