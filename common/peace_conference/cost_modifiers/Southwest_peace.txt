# Scripted cost modifiers for peace actions.
# See documentation.info file for more detailed scripting information.

peace_action_modifiers = {

    # Is Claimed by Taker
    generic_has_claim = {
        category = has_claim
        peace_action_type = take_states
        enable = {
            ROOT = {
                OR = {
                    original_tag = SCS
                    original_tag = BUF
                    original_tag = ROC
                    original_tag = LSP
                }
            }
            FROM.FROM.FROM = { is_claimed_by = ROOT.FROM }
            FROM.FROM = {
                
                OR = {
                    original_tag = SCS
                    original_tag = BUF
                    original_tag = ROC
                    original_tag = LSP
                }
            }
        }

        cost_multiplier = 0.4 # Reduce claims by 60% to make this nicer
    }

}