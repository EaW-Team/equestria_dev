sub_units = {
	ground_effect_vehicle = {
		sprite = destroyer
		map_icon_category = ship
		priority = 3
		active = yes
		type = { screen_ship }
		need = { ship_hull_ground_effect_vehicle = 1 }

		max_organisation = 35


		supply_consumption = 0.04
		critical_parts = { destroyed_ammo_storage broken_propeller on_fire rudder_jammed}
		critical_part_damage_chance_mult = 1
		
		hit_profile_mult = 4.0 # needed because speed is so high and vis is so low, and they corner like shit
	}
}
