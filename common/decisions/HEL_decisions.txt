HEL_annexation_decisions = {
	annexation_AET = {
		icon = generic_flags
		allowed = {
			tag = HEL
		}
		activation = {
			495 = {
				is_owned_and_controlled_by = ROOT
				} 
			495 = {
				is_not_claim_or_core_of_ROOT = yes
			}
			is_subject = no
			NOT = {
				has_country_flag = AET_CONQ
				country_exists = AET
			}
		}
		selectable_mission = yes
		days_mission_timeout = 60
		is_good = no
		cost = freedec?0
		fire_only_once = no
		cancel_trigger = {
			NOT = {
				495 = {
					is_owned_and_controlled_by = ROOT
				}
			}
		}
		complete_effect = {
			country_event = adelart.32
		}
		timeout_effect = {
			set_country_flag = AET_CONQ
			add_political_power = -30
			add_stability = -0.03
			add_war_support = -0.03
		}
		ai_will_do = {
			factor = 100
		}
	}
}