war_measures = {
	GFS_raise_claweto_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			915 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			915 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 915
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_claweto_kommando"
			custom_effect_tooltip = GFS_militia_1
			hidden_effect = {
				load_oob = "GFS_claweto_militia"
			}
		}
	}
	
	GFS_raise_beakenburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			913 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			913 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 913
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_beakenburg_kommando"
			custom_effect_tooltip = GFS_militia_2
			hidden_effect = {
				load_oob = "GFS_beakenburg_militia"
			}
		}
	}
	
	GFS_raise_arendtoria_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			912 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			912 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 912
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_arendtoria_kommando"
			custom_effect_tooltip = GFS_militia_3
			hidden_effect = {
				load_oob = "GFS_arendtoria_militia"
			}
		}
	}
	
	GFS_raise_mossiestadt_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			911 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			911 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 911
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_mossiestadt_kommando"
			custom_effect_tooltip = GFS_militia_4
			hidden_effect = {
				load_oob = "GFS_mossiestadt_militia"
			}
		}
	}
	
	GFS_raise_duifenberg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			914 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			914 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 914
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_duifenberg_kommando"
			custom_effect_tooltip = GFS_militia_5
			hidden_effect = {
				load_oob = "GFS_duifenberg_militia"
			}
		}
	}
}