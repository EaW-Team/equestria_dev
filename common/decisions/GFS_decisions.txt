war_measures = {
	GFS_raise_claweto_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			915 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			915 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 915
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_1_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 915
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_claweto_kommando"
			custom_effect_tooltip = GFS_militia_1_tp
			hidden_effect = {
				load_oob = "GFS_claweto_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_1_raised
			}
		}
	}
	
	GFS_raise_beakenburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			913 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			913 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 913
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_2_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 913
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_beakenburg_kommando"
			custom_effect_tooltip = GFS_militia_2_tp
			hidden_effect = {
				load_oob = "GFS_beakenburg_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_2_raised
			}
		}
	}
	
	GFS_raise_arendtoria_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			912 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			912 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 912
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_3_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 912
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_arendtoria_kommando"
			custom_effect_tooltip = GFS_militia_3_tp
			hidden_effect = {
				load_oob = "GFS_arendtoria_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_3_raised
			}
		}
	}
	
	GFS_raise_mossiestadt_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			911 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			911 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 911
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_4_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 911
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_mossiestadt_kommando"
			custom_effect_tooltip = GFS_militia_4_tp
			hidden_effect = {
				load_oob = "GFS_mossiestadt_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_4_raised
			}
		}
	}
	
	GFS_raise_duifenberg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			914 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			914 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 914
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_5_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 914
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_duifenberg_kommando"
			custom_effect_tooltip = GFS_militia_5_tp
			hidden_effect = {
				load_oob = "GFS_duifenberg_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_5_raised
			}
		}
	}
	
	GFS_raise_zweneg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			934 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			934 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 934
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_6_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 934
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_zweneg_kommando"
			custom_effect_tooltip = GFS_militia_6_tp
			hidden_effect = {
				load_oob = "GFS_zweneg_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_6_raised
			}
		}
	}
	
	GFS_raise_maritzburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			908 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			908 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			controls_state = 908
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_7_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 908
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_maritzburg_kommando"
			custom_effect_tooltip = GFS_militia_7_tp
			hidden_effect = {
				load_oob = "GFS_maritzburg_militia"
				clr_country_flag = GFS_raising_militias
				set_country_flag = GFS_militia_7_raised
			}
		}
	}
}