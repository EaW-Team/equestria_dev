war_measures = {
	GFS_raise_claweto_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			915 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			915 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			915 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_1_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 915
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_claweto_kommando"
			custom_effect_tooltip = GFS_militia_1_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				915 = { 
				    set_state_flag = GFS_militia_1_raised 
				    create_unit = {
						division = "name = \"Klaweto Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_beakenburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			913 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			913 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			913 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_2_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 913
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_beakenburg_kommando"
			custom_effect_tooltip = GFS_militia_2_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				913 = { 
				    set_state_flag = GFS_militia_2_raised 
				    create_unit = {
						division = "name = \"Beakenburg Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_arendtoria_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			912 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			912 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			912 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_3_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 912
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_arendtoria_kommando"
			custom_effect_tooltip = GFS_militia_3_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				912 = { 
				    set_state_flag = GFS_militia_3_raised 
				    create_unit = {
						division = "name = \"Ardentoria Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
					create_unit = {
						division = "name = \"Uilfontein Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_mossiestadt_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			911 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			911 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			911 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_4_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 911
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_mossiestadt_kommando"
			custom_effect_tooltip = GFS_militia_4_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				911 = { 
				    set_state_flag = GFS_militia_4_raised 
				    create_unit = {
						division = "name = \"Mossiestadt Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
					create_unit = {
						division = "name = \"Neiw Rottendendam\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_duifenberg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			914 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			914 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			914 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_5_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 914
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_duifenberg_kommando"
			custom_effect_tooltip = GFS_militia_5_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				915 = { 
				    set_state_flag = GFS_militia_5_raised 
				    create_unit = {
						division = "name = \"Duifenberg Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_zweneg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			934 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			934 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			934 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_6_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 934
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_zweneg_kommando"
			custom_effect_tooltip = GFS_militia_6_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				934 = { 
				    set_state_flag = GFS_militia_6_raised 
				    create_unit = {
						division = "name = \"Zweneg Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_maritzburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			908 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			908 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			908 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_7_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 908
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_maritzburg_kommando"
			custom_effect_tooltip = GFS_militia_7_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				908 = { 
				    set_state_flag = GFS_militia_7_raised 
				    create_unit = {
						division = "name = \"Maritzburg Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_rima_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			907 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			907 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			907 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_8_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 7
		highlight_states = {
			state = 907
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_rima_kommando"
			custom_effect_tooltip = GFS_militia_8_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				907 = { 
				    set_state_flag = GFS_militia_8_raised 
				    create_unit = {
						division = "name = \"Nieuw Rima Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
	
	GFS_raise_vanderbijlburg_kommando = {
		icon = generic_nationalism
		allowed = {
		    OR = {
			    original_tag = TSV
				original_tag = GFS
			}
		}
		available = {
		    if = {
			limit = { original_tag = TSV }
			906 = {
			    is_fully_controlled_by = TSV
			    }
			}
			if = {
			limit = { original_tag = GFS }
			906 = {
			    is_fully_controlled_by = GFS
			    }
			}
			NOT = {
			    has_country_flag = GFS_raising_militias
			}
		}
		visible = {
			has_defensive_war = yes
			has_idea = GFS_commandos
			906 = {
				is_controlled_by = ROOT
				NOT = { has_state_flag = GFS_militia_9_raised }
			}
			if = {
			limit = { original_tag = TSV }
			    any_enemy_country = {
				    NOT = { original_tag = GFS }
			    }
			}
			if = {
			limit = { original_tag = GFS }
			    any_enemy_country = {
				    NOT = { original_tag = TSV }
			    }
			}
			NOT = {
			    has_country_flag = GFS_militia_9_raised
			}
		}
		
		ai_will_do = {
			factor = 10
			modifier = {
				has_political_power < 150
				factor = 0
			}
		}
		
		cost = 10
		days_remove = 14
		highlight_states = {
			state = 906
		}
		complete_effect = {
		    hidden_effect = {
			    set_country_flag = GFS_raising_militias
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GFS_raise_vanderbijlburg_kommando"
			custom_effect_tooltip = GFS_militia_9_tp
			hidden_effect = {
				if = {
				    limit = { 
			            NOT = { 
						    has_template = "Kommando"
						}
					}
					division_template = {
	                name = "Kommando"
	                is_locked = yes
	                    regiments = {
		                    infantry = { x = 0 y = 0 }
		                    infantry = { x = 0 y = 1 }
	                    }
	                    support = {
                            recon = { x = 0 y = 0 }
	                    }
                    }
				}
				906 = { 
				    set_state_flag = GFS_militia_9_raised 
				    create_unit = {
						division = "name = \"Vanderbijlburg Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
					create_unit = {
						division = "name = \"Voëlstad Kommando\" division_template = \"Kommando\" start_experience_factor = 0.7 start_equipment_factor = 1.0"
						owner = ROOT
					}
				}
				clr_country_flag = GFS_raising_militias
			}
		}
	}
}