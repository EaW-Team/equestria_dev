
LSP_deal_with_famine = {
	LSP_famine_timer = {

		allowed = { tag = LSP }
		activation = { always = no } #Activated by event
		available = { #Will not happen if:
			hidden_trigger = {
				always = no
			}
		}
		days_mission_timeout = 120
		is_good = no
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout LSP_famine_timer"
			LSP_increase_famine = yes
			custom_effect_tooltip = LSP_recurring_mission
			if = {
				limit = {
					NOT = {
						has_idea = LSP_catastrophic_famine
					}
				}
				activate_mission = LSP_famine_timer
			}
		}
	}
	LSP_mayor_trade_for_food_from = {
        icon = rice_bowl
        targets = { SCS EQS CRY CHN OLE }
        target_trigger = {
            FROM = {
                OR = {
                    original_tag = CHN
                    original_tag = SCS
                    AND = {
                        original_tag = EQS
                        has_government = democratic
                    }
                    AND = {
                        original_tag = CRY
                        has_government = democratic
                    }
                    AND = {
                        original_tag = OLE
                        NOT = {
                            has_government = fascism
                            has_government = communism
                        }
                    }
                }
            }
        }
        visible = {
			FROM = {
                exists = yes
            }
        }
        cost = 40
        days_remove = 90
		fire_only_once = yes
        ai_will_do = {
            factor = 200
        }
		targeted_modifier = {
			tag = FROM
			cic_to_target_factor = 0.34
			mic_to_target_factor = 0.34
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LSP_mayor_trade_for_food_from target: [From.GetName]"
			LSP_reduce_famine = yes
        }
	}
}