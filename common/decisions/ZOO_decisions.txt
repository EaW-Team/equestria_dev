ZOO_railway_decisions_category = {
	ZOO_select_zamu_line_decision = {
		icon = generic_form_nation
		allowed = {
			original_tag = ZOO
		}
		fire_only_once = no
		highlight_states = {
			highlight_provinces = { 16348 16361 16372 16355 16354 16542 16546 16541 }
		}
		available = {
			NOT = {
				has_country_flag = ZOO_zamu_line_selected
			}
			if = {
				limit = {
					is_ai = yes
				}
				NOT = {
					has_country_flag = ZOO_zupinamba_line_selected
					has_country_flag = ZOO_zuba_line_selected
				}
			}
			custom_trigger_tooltip = {
				tooltip = ZOO_has_unlocked_this_level_tt
				check_variable = {
					var = ZOO_railway_zamu_line_level
					value = ZOO_unlocked_level_x_railroads
					compare = less_than
				}
			}		
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision form_zaikiria_decision"
			set_country_flag = ZOO_zamu_line_selected
			clr_country_flag = ZOO_zupinamba_line_selected
			clr_country_flag = ZOO_zuba_line_selected
		}
		ai_will_do = {
			factor = 200
		}
	}

	ZOO_select_zupinamba_line_decision = {
		icon = generic_form_nation
		allowed = {
			original_tag = ZOO
		}
		highlight_states = {
			highlight_provinces = { 16348 16337 16312 16284 16280 16273 16274 16207 }
		}
		fire_only_once = no
		available = {
			NOT = {
				has_country_flag = ZOO_zupinamba_line_selected
			}
			if = {
				limit = {
					is_ai = yes
				}
				NOT = {
					has_country_flag = ZOO_zamu_line_selected
					has_country_flag = ZOO_zuba_line_selected
				}
			}
			custom_trigger_tooltip = {
				tooltip = ZOO_has_unlocked_this_level_tt
				check_variable = {
					var = ZOO_railway_zupinamba_line_level
					value = ZOO_unlocked_level_x_railroads
					compare = less_than
				}
			}	
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision form_zaikiria_decision"
			clr_country_flag = ZOO_zamu_line_selected
			set_country_flag = ZOO_zupinamba_line_selected
			clr_country_flag = ZOO_zuba_line_selected
		}
		ai_will_do = {
			factor = 200
		}
	}

	ZOO_select_zuba_line_decision = {
		icon = generic_form_nation
		allowed = {
			original_tag = ZOO
		}
		fire_only_once = no
		highlight_states = {
			highlight_provinces = { 16348 16351 16371 4282 1713 16371 16350 16338 16238 }
		}
		available = {
			NOT = {
				has_country_flag = ZOO_zuba_line_selected
			}
			if = {
				limit = {
					is_ai = yes
				}
				NOT = {
					has_country_flag = ZOO_zamu_line_selected
					has_country_flag = ZOO_zupinamba_line_selected
				}
			}
			custom_trigger_tooltip = {
				tooltip = ZOO_has_unlocked_this_level_tt
				check_variable = {
					var = ZOO_railway_zuba_line_level
					value = ZOO_unlocked_level_x_railroads
					compare = less_than
				}
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision form_zaikiria_decision"
			clr_country_flag = ZOO_zamu_line_selected
			clr_country_flag = ZOO_zupinamba_line_selected
			set_country_flag = ZOO_zuba_line_selected
		}
		ai_will_do = {
			factor = 200
		}
	}
}

foreign_politics = {
	ZOO_invade_MEL_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		visible = {
			has_completed_focus = zoo_army_3_2
			MEL = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = MEL
			}
		}

		targets = {
			MEL
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 50
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = MEL
				}
			}
			declare_war_on = {
				type = annex_everything
				target = MEL
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				MEL = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
			}
		}
	}

	ZOO_invade_TAI_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		visible = {
			has_completed_focus = zoo_army_3_2
			TAI = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = TAI
			}
		}

		targets = {
			TAI
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 50
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = TAI
				}
			}
			declare_war_on = {
				type = annex_everything
				target = TAI
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				TAI = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
			}
		}
	}

	ZOO_invade_MQA_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 0 #TODO
		}

		visible = {
			has_completed_focus = zoo_army_5_1
			MQA = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = MQA
			}
		}

		targets = {
			MQA
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 50
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = MQA
				}
			}
			declare_war_on = {
				type = annex_everything
				target = MQA
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				MQA = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
			}
		}
	}
}