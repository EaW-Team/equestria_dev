ZOO_railway_decisions_category = {
	ZOO_environmental_decay_decision = {
		icon = generic_civil_support
		allowed = {
			tag = ZOO
		}
		activation = {
			has_completed_focus = zoo_railway_1_1
		}
		available = {
			always = no
		}
		visible = {
			OR = {
				AND = {
					has_idea = ZOO_environmental_damage_4
					has_completed_focus = zoo_railway_8_3
				}
				AND = {
					NOT = {
						has_completed_focus = zoo_railway_8_1
						has_completed_focus = zoo_railway_8_2
						has_idea = ZOO_environmental_damage_4
					}
				}
			}
			NOT = {
				has_completed_focus = zoo_railway_8_1
			}
		}

		is_good = no
		days_re_enable = 0

		days_mission_timeout = 365

		fire_only_once = no

		cancel_if_not_visible = yes

		timeout_effect = {
			if = {
				limit = {
					has_idea = ZOO_environmental_damage_start
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_start
					add_idea = ZOO_environmental_damage_1
				}
				activate_mission = ZOO_environmental_decay_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_1
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_1
					add_idea = ZOO_environmental_damage_2
				}
				activate_mission = ZOO_environmental_decay_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_2
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_2
					add_idea = ZOO_environmental_damage_3
				}
				activate_mission = ZOO_environmental_decay_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_3
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_3
					add_idea = ZOO_environmental_damage_4
				}
				if = {
					limit = { has_completed_focus = zoo_railway_8_3 }
					activate_mission = ZOO_environmental_decay_decision
				}
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_4
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_4
					add_idea = ZOO_environmental_damage_5
				}
			}
		}
	}
	ZOO_environmental_repair_decision = {
		icon = generic_civil_support
		allowed = {
			tag = ZOO
		}
		activation = {
			has_completed_focus = zoo_railway_8_1
		}

		visible = {
			has_completed_focus = zoo_railway_8_1
			NOT = {
				has_idea = ZOO_environmental_damage_handled
			}
		}

		available = {
			always = no
		}

		is_good = yes

		days_mission_timeout = 120

		fire_only_once = no
		days_re_enable = 0

		cancel_if_not_visible = yes

		timeout_effect = {
			if = {
				limit = {
					has_idea = ZOO_environmental_damage_1
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_1
					add_idea = ZOO_environmental_damage_handled
				}
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_2
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_2
					add_idea = ZOO_environmental_damage_1
				}
				activate_mission = ZOO_environmental_repair_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_3
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_3
					add_idea = ZOO_environmental_damage_2
				}
				activate_mission = ZOO_environmental_repair_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_4
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_4
					add_idea = ZOO_environmental_damage_3
				}
				activate_mission = ZOO_environmental_repair_decision
			}
			else_if = {
				limit = {
					has_idea = ZOO_environmental_damage_5
				}
				swap_ideas = {
					remove_idea = ZOO_environmental_damage_5
					add_idea = ZOO_environmental_damage_4
				}
				activate_mission = ZOO_environmental_repair_decision
			}
		}
	}
}

foreign_politics = {
	ZOO_partition_zaikiria_decision = {
		icon = generic_form_nation
		allowed = {
			original_tag = ZOO
		}
		fire_only_once = no
		visible = {
			has_completed_focus = zoo_north_2_2
		}
		available = {
			OR = {
				owns_state = 1085
				owns_state = 1086
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ZOO_partition_zaikiria_decision"
			if = {
				limit = { 
					NOT = {
						country_exists = NVA
					}
				}
				release_on_controlled = NVA
			}
			if = {
				limit = { 
					owns_state = 1086
				}
				1086 = {
					transfer_state_to = NVA
					add_core_of = NVA
				}
			}
			if = {
				limit = { 
					NOT = {
						country_exists = KHM
					}
				}
				release_on_controlled = KHM
			}
			if = {
				limit = { 
					owns_state = 1085
				}
				1085 = {
					transfer_state_to = KHM
					add_core_of = KHM
				}
			}
			set_country_flag = ZOO_partitioned_zaikiria
		}
		ai_will_do = {
			factor = 200
		}
	}

	ZOO_release_zaikiria_decision = {
		icon = generic_form_nation
		allowed = {
			original_tag = ZOO
		}
		fire_only_once = yes
		visible = {
			has_completed_focus = zoo_north_2_1
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = form_zaikiria_decision_req_tt
				all_of_scopes = {
					array = KHM.core_states
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
						PREV = { is_fully_controlled_by = PREV }
					}
				}
				all_of_scopes = {
					array = KXN.core_states
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
						PREV = { is_fully_controlled_by = PREV }
					}
				}
				all_of_scopes = {
					array = NVA.core_states
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
						PREV = { is_fully_controlled_by = PREV }
					}
				}
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ZOO_release_zaikiria_decision"
			custom_effect_tooltip = ZOO_release_zaikiria_decision_tt
			hidden_effect = {
				release_puppet_on_controlled = NVA
				NVA = {
					set_cosmetic_tag = United_Zaikiria_monarchy
					for_each_scope_loop = {
						array = KXN.original_cores
						add_core_of = NVA
					}
					for_each_scope_loop = {
						array = KHM.original_cores
						add_core_of = NVA
					}
				}
			}
			set_country_flag = ZOO_established_zaikirian_subject
			
		}
		ai_will_do = {
			factor = 200
		}
	}

	ZOO_invade_MEL_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_army_3_2
			MEL = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = MEL
			}
		}

		targets = {
			MEL
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = MEL
				}
			}
			declare_war_on = {
				type = annex_everything
				target = MEL
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				MEL = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = MEL
					value = 1000
				}
			}
		}
	}

	ZOO_invade_TAI_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_army_3_2
			TAI = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = TAI
			}
		}

		targets = {
			TAI
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = TAI
				}
			}
			declare_war_on = {
				type = annex_everything
				target = TAI
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				TAI = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = TAI
					value = 1000
				}
			}
		}
	}

	ZOO_invade_ZLU_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_army_5_1
			ZLU = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = ZLU
			}
		}

		targets = {
			ZLU
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = ZLU
				}
			}
			declare_war_on = {
				type = annex_everything
				target = ZLU
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				ZLU = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = ZLU
					value = 1000
				}
			}
		}
	}

	ZOO_invade_ROA_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_west_2_3
			ROA = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = ROA
			}
		}

		targets = {
			ROA
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = ROA
				}
			}
			declare_war_on = {
				type = annex_everything
				target = ROA
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				ROA = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = ROA
					value = 1000
				}
			}
		}
	}

	ZOO_invade_TSV_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_west_2_1
			TSV = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = TSV
			}
		}

		targets = {
			TSV
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = TSV
				}
			}
			declare_war_on = {
				type = annex_everything
				target = TSV
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				TSV = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = TSV
					value = 1000
				}
			}
		}
	}

	ZOO_invade_GFS_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_west_2_1
			GFS = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = GFS
			}
		}

		targets = {
			GFS
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = GFS
				}
			}
			declare_war_on = {
				type = annex_everything
				target = GFS
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				GFS = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = GFS
					value = 1000
				}
			}
		}
	}

	ZOO_invade_KXN_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			OR = {
				has_completed_focus = zoo_north_1_1
				has_completed_focus = zoo_north_2_1
			}
			KXN = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
					is_subject = yes
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = KXN
			}
		}

		targets = {
			KXN
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = KXN
				}
			}
			declare_war_on = {
				type = annex_everything
				target = KXN
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				KXN = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = KXN
					value = 1000
				}
			}
		}
	}

	ZOO_invade_NVA_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_north_2_1
			NVA = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
					is_subject = yes
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = NVA
			}
		}

		targets = {
			NVA
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = NVA
				}
			}
			declare_war_on = {
				type = annex_everything
				target = NVA
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				NVA = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = NVA
					value = 1000
				}
			}
		}
	}

	ZOO_invade_KHM_decision = {
		icon = infiltrate_state
		allowed = {
			original_tag = ZOO
		}
		ai_will_do = {
			factor = 200
		}

		available = {
			NOT = {
				has_country_flag = ZOO_preparing_for_war
			}
		}

		visible = {
			has_completed_focus = zoo_north_2_1
			KHM = {
				exists = yes
				NOT = {
					has_war_with = ZOO
					is_ally_with = ZOO
					is_subject = yes
				}
			}
		}

		target_trigger = {
			FROM = {
				original_tag = KHM
			}
		}

		targets = {
			KHM
		}

		modifier = {
			political_power_factor = -0.05
		}

		cost = 25
		days_remove = 30
		fire_only_once = yes

		war_with_on_remove = NVA
		war_with_on_complete = NVA
		war_with_on_timeout = NVA

		war_with_target_on_remove = yes
		war_with_target_on_complete = yes
		war_with_target_on_timeout = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove KIS_invade_NVA_decision"
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = KHM
				}
			}
			declare_war_on = {
				type = annex_everything
				target = KHM
			}
			clr_country_flag = ZOO_preparing_for_war
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision KIS_invade_NVA_decision"
			hidden_effect = {
				set_country_flag = ZOO_preparing_for_war
				KHM = {
					add_ai_strategy = {
						type = prepare_for_war
						id = ZOO
						value = 1000
					}
				}
				add_ai_strategy = {
					type = prepare_for_war
					id = KHM
					value = 1000
				}
			}
		}
	}
}