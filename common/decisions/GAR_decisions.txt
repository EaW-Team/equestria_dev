military_training_category = {
	GAR_to_arms = {
		icon = rifles
		allowed = { tag = GAR }
		available = {
			has_war = yes
		}
		visible = {
			has_completed_focus = GAR_king_civilian_9
			NOT = { has_idea = GAR_to_arms_idea }
		}
		cost = 15
		fire_only_once = yes
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GAR_to_arms"
			add_ideas = GAR_to_arms_idea
		}
		ai_will_do = {
			factor = 10
		}
	}

	GAR_red_alert = {
		icon = rifles
		allowed = { tag = GAR }
		available = {
			has_war = yes
		}
		visible = {
			has_completed_focus = GAR_king_military_9
			NOT = {
				has_idea = scraping_the_barrel
				has_idea = tot_economic_mobilisation
			}
		}
		cost = 100
		days_remove = 70
		fire_only_once = yes
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GAR_to_arms"
			if = {
				limit = {
					has_idea = disarmed_nation
				}
				add_ideas = volunteer_only
			}
			else_if = {
				limit = {
					has_idea = volunteer_only
				}
				add_ideas = limited_conscription
			}
			else_if = {
				limit = {
					has_idea = limited_conscription
				}
				add_ideas = extensive_conscription
			}
			else_if = {
				limit = {
					has_idea = extensive_conscription
				}
				add_ideas = service_by_requirement
			}
			else_if = {
				limit = {
					has_idea = service_by_requirement
				}
				add_ideas = all_adults_serve
			}
			else_if = {
				limit = {
					has_idea = all_adults_serve
				}
				add_ideas = scraping_the_barrel
			}

			if = {
				limit = {
					has_idea = civilian_economy
				}
				add_ideas = low_economic_mobilisation
			}
			else_if = {
				limit = {
					has_idea = low_economic_mobilisation
				}
				add_ideas = partial_economic_mobilisation
			}
			else_if = {
				limit = {
					has_idea = partial_economic_mobilisation
				}
				add_ideas = war_economy
			}
			else_if = {
				limit = {
					has_idea = war_economy
				}
				add_ideas = tot_economic_mobilisation
			}
		}
		ai_will_do = {
			factor = 10
		}
	}

	GAR_war_plan = {
		icon = rifles
		allowed = { tag = GAR }
		available = {
			has_war = yes
		}
		visible = {
			has_completed_focus = GAR_compromise_military_10
			NOT = { has_idea = GAR_war_plan }
		}
		cost = 50
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GAR_war_plan"
			add_ideas = GAR_war_plan
		}
		ai_will_do = {
			factor = 10
		}
	}

	GAR_war_plan_disable = {
		icon = rifles
		allowed = { tag = GAR }
		available = {
			has_war = yes
		}
		visible = {
			has_completed_focus = GAR_compromise_military_10
			has_idea = GAR_war_plan
		}
		cost = 50
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GAR_war_plan_disable"
			remove_ideas = GAR_war_plan
		}
		ai_will_do = {
			factor = 10
		}
	}
}
