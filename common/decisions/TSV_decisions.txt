unite_chisimbi_category = {

	unite_chisimbi_decision_TSV = {
		icon = generic_form_nation
		allowed = {
			original_tag = TSV
		}
		fire_only_once = yes
		available = {
			is_subject = no
			GFS = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			ROA = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			TSV = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			IBZ = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			ZLU = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			GIF = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
		}

		visible = {
			original_tag = TSV
			is_subject = no
			NOT = { has_global_flag = chisimbi_has_united }
		}

		complete_effect = {
			if = { 
				limit = { TSV = { has_government = fascism } } 
				set_cosmetic_tag = TSV_chisimbi_unified
			 }

			else = { set_cosmetic_tag = Generic_chisimbi_cosmetic_tag_placeholder }
			
			GFS = { add_to_ROOT_potential_original_cores = yes }
			ROA = { add_to_ROOT_potential_original_cores = yes }
			TSV = { add_to_ROOT_potential_original_cores = yes }
			IBZ = { add_to_ROOT_potential_original_cores = yes }
			ZLU = { add_to_ROOT_potential_original_cores = yes }
			GIF = { add_to_ROOT_potential_original_cores = yes }

			# Annex subjects in the region
			every_subject_country = {
				limit = {
					OR = {
						original_tag = GFS
						original_tag = ROA
						original_tag = TSV
						original_tag = IBZ
						original_tag = ZLU
						original_tag = GIF
					}
				}
				ROOT = { 
					annex_country = {
						target = PREV
					}
				}
			}

			if = {
				limit = { is_ai = yes }
				every_owned_state = {
					limit = {
						NOT = {
							is_core_of = ROOT
						}
					}
					add_compliance = 20
				}
			}

			hidden_effect = {
				set_global_flag = chisimbi_has_united
			}
		}

		ai_will_do = {
			factor = 300
		}
	}

}