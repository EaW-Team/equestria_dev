DEP_Spiders = {
	DEP_send_Delegation = {
		icon = griffon_bandits
		visible = {
			has_country_flag = DEP_Spider_story_begin
		}		                        
		allowed = { tag = DEP }
		fire_only_once = yes              
		cost = 25
		days_remove = 5
		remove_effect = {
			country_event = deponya_spider.4 
		}		
	} 
}
