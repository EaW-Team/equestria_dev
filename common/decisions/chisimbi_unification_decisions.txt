chisimbi_unification_decisions = {

	chisimbi_consolidate_south = {
		icon = generic_ignite_civil_war
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
			}
		}
		available = {
			is_subject = no
			NOT = {
				has_country_flag = chisimbi_internal_consolidated
				has_war_with = GFS
				has_war_with = ROA
			}
		}

		visible = {
			OR = {
				original_tag = GFS
				original_tag = ROA
			}
			is_subject = no
			NOT = {
				has_country_flag = chisimbi_internal_consolidated
			}
			
		}
		fire_only_once = yes
		cost = 25

		ai_will_do = {
			factor = 250
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_consolidate_south"
			set_country_flag = chisimbi_internal_consolidated
			ROOT = {
				division_template = {
					name = "Veteran Infantry"
					template_counter = 25
					regiments = {
						infantry = { x = 0 y = 0 }
						infantry = { x = 0 y = 1 }
						infantry = { x = 0 y = 2 }
						infantry = { x = 0 y = 3 }
						infantry = { x = 1 y = 0 }
						infantry = { x = 1 y = 1 }
						infantry = { x = 1 y = 2 }
						infantry = { x = 1 y = 3 }
					}
					support = {
						artillery = { x = 0 y = 0 }
					}
				}
				capital_scope = {
					create_unit = { division = "division_template = \"Veteran Infantry\" start_equipment_factor = 1.0 start_experience_factor = 0.24" owner = ROOT count = 3 }
				}
				if = {
					limit = { tag = ROA }
					set_division_template_cap = { 
						division_template = "Legio Praetoriani" 
						division_cap = 2
					}
					capital_scope = {
						create_unit = { division = "division_template = \"Legio Praetoriani\" start_equipment_factor = 1.0 start_experience_factor = 0.4" owner = ROOT count = 1 }
					}
				}
				else = {
					capital_scope = {
						create_unit = { division = "division_template = \"Veteran Infantry\" start_equipment_factor = 1.0 start_experience_factor = 0.4" owner = ROOT count = 1 }
					}
				}
			}
			if = {
				limit = {
					original_tag = ROA
				}
				add_research_slot = 1
				add_doctrine_cost_reduction = {
					name = land_doc_bonus
					cost_reduction = 0.5
					uses = 1
					category = land_doctrine
				}
				if = {
					limit = { has_dlc = "No Step Back" }
					add_ideas = flexible_organization_spirit
				}
			}
			if = {
				limit = {
					original_tag = GFS
				}
				add_state_core = 193
				add_state_core = 329
			}
			every_owned_state = {
				limit = { NOT = { is_core_of = ROOT } }
				add_compliance = 30
			}
		}
	}

	chisimbi_consolidate_north = {
		icon = generic_ignite_civil_war
		allowed = {
			OR = {
				original_tag = IBZ
				original_tag = TSV
			}
		}
		available = {
			is_subject = no
			NOT = {
				has_country_flag = chisimbi_internal_consolidated
				has_war_with = IBZ
				has_war_with = TSV
			}
		}

		visible = {
			OR = {
				original_tag = IBZ
				original_tag = TSV
			}
			is_subject = no
			NOT = {
				has_country_flag = chisimbi_internal_consolidated
			}
			
		}
		fire_only_once = yes
		cost = 25

		ai_will_do = {
			factor = 250
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_consolidate_north"
			set_country_flag = chisimbi_internal_consolidated
			ROOT = {
				division_template = {
					name = "Veteran Infantry"
					template_counter = 25
					regiments = {
						infantry = { x = 0 y = 0 }
						infantry = { x = 0 y = 1 }
						infantry = { x = 0 y = 2 }
						infantry = { x = 0 y = 3 }
						infantry = { x = 1 y = 0 }
						infantry = { x = 1 y = 1 }
						infantry = { x = 1 y = 2 }
						infantry = { x = 1 y = 3 }
					}
					support = {
						artillery = { x = 0 y = 0 }
					}
				}
				capital_scope = {
					create_unit = { division = "division_template = \"Veteran Infantry\" start_equipment_factor = 1.0 start_experience_factor = 0.4" owner = ROOT count = 5 }
				}
			}
			if = {
				limit = {
					original_tag = IBZ
				}
				add_research_slot = 1
				increase_society_development = yes
			}
			if = {
				limit = {
					original_tag = TSV
				}
				add_equipment_to_stockpile = { type = infantry_equipment_0 amount = 1500 producer = TSV }
				add_equipment_to_stockpile = { type = support_equipment amount = 250 producer = TSV }
			}
			every_owned_state = {
				limit = { NOT = { is_core_of = ROOT } }
				add_compliance = 30
			}
		}
	}

	chisimbi_start_central_conflict = {
		icon = generic_ignite_civil_war
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
			}
		}
		available = {
			date > 1008.06.01
			has_war = no
			is_subject = no
			has_country_flag = chisimbi_internal_consolidated
		}

		visible = {
			if = {
				limit = { # South
					OR = {
						original_tag = GFS
						original_tag = ROA
					}
				}
				OR = { # Any north exists
					TSV = {
						AND = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					IBZ = {
						AND = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
				}
			}
			else = { # North
				OR = { # Any south exists
					GFS = {
						AND = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					ROA = {
						AND = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
				}
			}
			is_subject = no
			
		}
		fire_only_once = yes
		war_with_target_on_remove = yes
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 250
		}
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_start_central_conflict"
			ROOT = {
                add_ai_strategy = {
                    type = prepare_for_war
                    id = ROOT
                    value = 1000
                }
            }
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_start_central_conflict"
			hidden_effect = {
				set_global_flag = chisimbi_central_conflict_started
			}
			if = {
				limit = {
					OR = {
						original_tag = ROA
						original_tag = GFS
					}
				}
				if = {
					limit = {
						TSV = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					declare_war_on = {
						target = TSV
						type = annex_everything
					}
				}
				if = {
					limit = {
						IBZ = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					declare_war_on = {
						target = IBZ
						type = annex_everything
					}
				}
			}
			if = {
				limit = {
					OR = {
						original_tag = IBZ
						original_tag = TSV
					}
				}
				if = {
					limit = {
						GFS = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					declare_war_on = {
						target = GFS
						type = annex_everything
					}
				}
				if = {
					limit = {
						ROA = {
							exists = yes
							NOT = { is_subject_of = ROOT }
						}
					}
					declare_war_on = {
						target = ROA
						type = annex_everything
					}
				}
			}
		}
	}

	chisimbi_zeblu_attack_center = {
		icon = generic_ignite_civil_war
		allowed = {
			original_tag = ZLU
		}
		available = {
			OR = {
				date > 1009.06.01
				has_global_flag = chisimbi_central_conflict_started
			}
			has_war = no
			is_subject = no
		}

		visible = {
			original_tag = ZLU
			has_war = no
			is_subject = no
			
		}
		fire_only_once = yes
		war_with_target_on_remove = yes
		cost = 25
		
		days_remove = 180
		ai_will_do = {
			factor = 250
		}
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_zeblu_attack_center"
			add_ideas = chisimbi_unification_wars
			ROOT = {
                add_ai_strategy = {
                    type = prepare_for_war
                    id = ROOT
                    value = 1000
                }
            }
			ROOT = {
				division_template = {
					name = "Levy Warband"
					template_counter = 25
					regiments = {
						infantry = { x = 0 y = 0 }
						infantry = { x = 0 y = 1 }
						infantry = { x = 0 y = 2 }
						infantry = { x = 1 y = 0 }
						infantry = { x = 1 y = 1 }
						infantry = { x = 1 y = 2 }
					}
				}
				capital_scope = {
					create_unit = { division = "division_template = \"Levy Warband\" start_equipment_factor = 1.0 start_experience_factor = 0.15" owner = ROOT count = 4 }
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_zeblu_attack_center"
			hidden_effect = {
				set_global_flag = chisimbi_zeblu_attacked
			}
			if = {
				limit = { # Tzhimane Confederacy
					TSV = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = TSV
					type = annex_everything
				}
			}
			if = {
				limit = { # Ibhayiz
					IBZ = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = IBZ
					type = annex_everything
				}
			}
			if = {
				limit = { # Imba-Zaru
					GFS = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = GFS
					type = annex_everything
				}
			}
			if = {
				limit = { # Roam
					ROA = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = ROA
					type = annex_everything
				}
			}
		}
	}

	chisimbi_twizanyika_attack_center = {
		icon = generic_ignite_civil_war
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
			}
		}
		available = {
			OR = {
				date > 1009.06.01
				has_global_flag = chisimbi_zeblu_attacked
			}
			has_war = no
			is_subject = no
			
		}

		visible = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
			}
			is_subject = no
		}
		
		fire_only_once = yes
		war_with_target_on_remove = yes
		targets = { GIF }
        targets_dynamic = yes
        target_trigger = {
            FROM = { 
				original_tag = GIF 
				NOT = { is_subject_of = ROOT }
			}
        }
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 250
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_twizanyika_attack_center"
			if = {
				limit = { GIF = { NOT = { has_idea = chisimbi_unification_wars }}}
				GIF = { add_ideas = chisimbi_unification_wars }
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_twizanyika_attack_center"
			declare_war_on = {
				target = GIF
				type = annex_everything
			}
			set_global_flag = chisimbi_twizanyika_attacked
		}
	}

	chisimbi_twizanyika_backstab_center = {
		icon = generic_ignite_civil_war
		allowed = {
			original_tag = GIF
		}
		available = {
			OR = {
				date > 1009.06.01
				has_global_flag = chisimbi_zeblu_attacked
			}
			has_war = no
			is_subject = no
			
		}

		visible = {
			original_tag = GIF
			is_subject = no
		}
		
		fire_only_once = yes
		war_with_target_on_remove = yes
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 250
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete chisimbi_twizanyika_backstab_center"
			if = {
				limit = { GIF = { NOT = { has_idea = chisimbi_unification_wars }}}
				GIF = { add_ideas = chisimbi_unification_wars }
			}
			ROOT = {
                add_ai_strategy = {
                    type = prepare_for_war
                    id = ROOT
                    value = 1000
                }
            }
			ROOT = {
				division_template = {
					name = "Mercenary Band"
					template_counter = 25
					regiments = {
						infantry = { x = 0 y = 0 }
						infantry = { x = 0 y = 1 }
						infantry = { x = 0 y = 2 }
						infantry = { x = 1 y = 0 }
						infantry = { x = 1 y = 1 }
						infantry = { x = 1 y = 2 }
					}
				}
				capital_scope = {
					create_unit = { division = "division_template = \"Mercenary Band\" start_equipment_factor = 1.0 start_experience_factor = 0.15" owner = ROOT count = 2 }
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_twizanyika_backstab_center"
			if = {
				limit = { # Tzhimane Confederacy
					TSV = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = TSV
					type = annex_everything
				}
			}
			if = {
				limit = { # Ibhayiz
					IBZ = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = IBZ
					type = annex_everything
				}
			}
			if = {
				limit = { # Imba-Zaru
					GFS = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = GFS
					type = annex_everything
				}
			}
			if = {
				limit = { # Roam
					ROA = {
						exists = yes
						NOT = { is_subject_of = ROOT }
					}
				}
				declare_war_on = {
					target = ROA
					type = annex_everything
				}
			}
		}
	}

}

unite_chisimbi_category = {

	unite_chisimbi_decision = {
		icon = generic_form_nation
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			is_subject = no
			GFS = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			ROA = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			TSV = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			IBZ = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			ZLU = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
			GIF = {
				all_original_cores_owned_by_ROOT_or_subject = yes
			}
		}

		visible = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
			is_subject = no
			NOT = { has_global_flag = chisimbi_has_united }
		}

		complete_effect = {
			if = {
				limit = { NOT = { tag = ROA } }
				set_cosmetic_tag = Generic_chisimbi_cosmetic_tag_placeholder
			}
			GFS = { add_to_ROOT_potential_original_cores = yes }
			ROA = { add_to_ROOT_potential_original_cores = yes }
			TSV = { add_to_ROOT_potential_original_cores = yes }
			IBZ = { add_to_ROOT_potential_original_cores = yes }
			ZLU = { add_to_ROOT_potential_original_cores = yes }
			GIF = { add_to_ROOT_potential_original_cores = yes }

			# Annex subjects in the region
			every_subject_country = {
				limit = {
					OR = {
						original_tag = GFS
						original_tag = ROA
						original_tag = TSV
						original_tag = IBZ
						original_tag = ZLU
						original_tag = GIF
					}
				}
				ROOT = { 
					annex_country = {
						target = PREV
					}
				}
			}

			hidden_effect = {
				set_global_flag = chisimbi_has_united
			}
		}

		ai_will_do = {
			factor = 300
		}
	}

}

chisimbi_expansion_category = {

	chisimbi_expand_zongolese = {
		icon = generic_nationalism
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			date > 1010.10.01
			is_subject = no
			has_war = no
		}

		visible = {
			is_subject = no
		}

		fire_only_once = yes
		war_with_target_on_remove = yes

		targets = { ZOO }

        targets_dynamic = yes

        target_trigger = {
            FROM = { 
				tag = ZOO
				NOT = { is_subject_of = ROOT } 
			}
        }
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 250
		}

		complete_effect = {
			add_ai_strategy = {
                type = prepare_for_war
                id = MEL
                value = 1000
            }
			MEL = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = 1000
				}
			}
			add_ai_strategy = {
                type = prepare_for_war
                id = TAI
                value = 1000
            }
			TAI = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = 1000
				}
			}
			add_ai_strategy = {
                type = prepare_for_war
                id = ZOO
                value = 1000
            }
			ZOO = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = 1000
				}
			}
		}
		
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_expand_zongolese"
			add_ai_strategy = {
                type = prepare_for_war
                id = ZOO
                value = -1000
            }
			ZOO = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = -1000
				}
			}
			declare_war_on = {
				target = ZOO
				type = annex_everything
			}
			add_ai_strategy = {
                type = prepare_for_war
                id = TAI
                value = -1000
            }
			TAI = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = -1000
				}
			}
			declare_war_on = {
				target = TAI
				type = annex_everything
			}
			add_ai_strategy = {
                type = prepare_for_war
                id = MEL
                value = -1000
            }
			MEL = {
				add_ai_strategy = {
					type = prepare_for_war
					id = ROOT
					value = -1000
				}
			}
			declare_war_on = {
				target = MEL
				type = annex_everything
			}
		}
	}
	
	chisimbi_consolidate_zongo = {
		icon = generic_form_nation
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			is_subject = no
			ZOO = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
			TAI = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
			MEL = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
		}
		visible = {
			has_global_flag = chisimbi_has_united
		}
		
		ai_will_do = {
			factor = 250
		}
		
		cost = 25
		
		fire_only_once = yes
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision chisimbi_consolidate_zongo"
			ZOO = { add_to_ROOT_potential_original_cores = yes }
			TAI = { add_to_ROOT_potential_original_cores = yes }
			MEL = { add_to_ROOT_potential_original_cores = yes }

			hidden_effect = {
				set_country_flag = chisimbi_has_conquered_middle_southeast

				if = {
					limit = {
						original_tag = ROA
					}
					news_event = { 
						id = newsroam.1 	
						hours = 1 
					}
					set_cosmetic_tag = ROA_imperial_cosmetic_tag
				}
			}
		}
	}
	
	chisimbi_expand_far_south = {
		icon = generic_nationalism
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			date > 1011.01.01
			is_subject = no
			has_war = no
		}

		visible = {
			is_subject = no
			has_country_flag = chisimbi_has_conquered_middle_southeast
		}

		fire_only_once = yes
		war_with_target_on_remove = yes
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 250
		}
		
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_expand_far_south"
			declare_war_on = {
				target = IST
				type = annex_everything
			}
			declare_war_on = {
				target = IDO
				type = annex_everything
			}
			declare_war_on = {
				target = DEL
				type = annex_everything
			}
		}
	}

	chisimbi_expand_zakiria = {
		icon = generic_nationalism
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			date > 1012.01.01
			is_subject = no
			has_war = no
		}

		visible = {
			is_subject = no
			has_country_flag = chisimbi_has_conquered_middle_southeast
			OR = {
				KXN = { 
					exists = yes
					is_subject = no
				}
				KHM = { 
					exists = yes
					is_subject = no
				}
				NVA = { 
					exists = yes
					is_subject = no
				}
			}
		}
		fire_only_once = yes
		war_with_target_on_remove = yes
		cost = 25
		
		days_remove = 5
		ai_will_do = {
			factor = 250
		}
		
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_expand_zakiria"
			if = {
				limit = {
					KXN = { 
						exists = yes
						is_subject = no
					}
				}
				declare_war_on = {
					target = KXN
					type = annex_everything
				}
			}
			if = {
				limit = {
					KHM = { 
						exists = yes
						is_subject = no
					}
				}
				declare_war_on = {
					target = KHM
					type = annex_everything
				}
			}
			if = {
				limit = {
					NVA = { 
						exists = yes
						is_subject = no
					}
				}
				declare_war_on = {
					target = NVA
					type = annex_everything
				}
			}
		}
	}

	chisimbi_consolidate_far_south = {
		icon = generic_form_nation
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			is_subject = no
			IST = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
			IDO = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
			DEL = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
		}
		visible = {
			has_country_flag = chisimbi_has_conquered_middle_southeast
		}
		
		cost = 25
		
		fire_only_once = yes
		
		ai_will_do = {
			factor = 250
		}
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision chisimbi_consolidate_far_south"
			IST = { add_to_ROOT_potential_original_cores = yes }
			IDO = { add_to_ROOT_potential_original_cores = yes }
			DEL = { add_to_ROOT_potential_original_cores = yes }
		}
	}

	chisimbi_consolidate_the_isles = {		#no conquest, because AI can't manage the navy now, but player can
		icon = generic_form_nation
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			is_subject = no
			MQA = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
			YAN = {
				all_core_state = {
					owner = {
						tag = ROOT
					}
				}
			}
		}
		visible = {
			has_global_flag = chisimbi_has_united
			has_country_flag = chisimbi_has_conquered_middle_southeast
		}
		
		cost = 25
		
		fire_only_once = yes
		
		ai_will_do = {
			factor = 250
		}
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision chisimbi_consolidate_the_isles"
			MQA = { add_to_ROOT_potential_original_cores = yes }
			YAN = { add_to_ROOT_potential_original_cores = yes }
		}
	}

	chisimbi_expand_kiria = {
		icon = generic_nationalism
		allowed = {
			OR = {
				original_tag = GFS
				original_tag = ROA
				original_tag = TSV
				original_tag = IBZ
				original_tag = ZLU
				original_tag = GIF
			}
		}
		available = {
			date > 1013.00.01
			is_subject = no
			NOT = {
				has_war_with = ZOO
				has_war_with = TAI
				has_war_with = MEL
				has_war_with = KIR
				has_war_with = KIS
				has_war_with = KIC
			}
		}

		visible = {
			is_subject = no
		}
		fire_only_once = yes
		war_with_target_on_remove = yes
		targets = { KIR KIS KIC }
        targets_dynamic = yes
        target_trigger = {
            FROM = { 
				OR = {
					original_tag = KIR
					original_tag = KIS
					original_tag = KIC
				} 
				NOT = { is_subject_of = ROOT }
			}
			NOT = { has_war_with = FROM }
        }
		cost = 25
		
		days_remove = 30
		ai_will_do = {
			factor = 200
		}
		
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove chisimbi_expand_kiria"
			declare_war_on = {
				target = KIR
				type = puppet_wargoal_focus
			}
			declare_war_on = {
				target = KIC
				type = puppet_wargoal_focus
			}
			declare_war_on = {
				target = KIS
				type = puppet_wargoal_focus
			}
		}
	}

}