BAK_dev_category = {
	BAK_inddevdec_one = {

		icon = generic_industry

		available = {
			OR = {
				has_completed_focus = BAK_big_fac_output
				has_completed_focus = BAK_small_eff_reten
			}
		}

		visible = {
			has_idea = pre_industrial_society
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 547

		modifier = {
			civilian_factory_use = 2
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_inddevdec_one"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_inddevdec_one"
			swap_ideas = {
				add_idea = outdated_industry
				remove_idea = pre_industrial_society
			}
		}
	}
	BAK_inddevdec_two = {

		icon = generic_industry

		available = {
			OR = {
				has_completed_focus = BAK_big_prod_eff_base
				has_completed_focus = BAK_small_prod_eff
			}
		}

		visible = {
			has_idea = outdated_industry
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 547

		modifier = {
			civilian_factory_use = 4
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_inddevdec_two"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_inddevdec_two"
			swap_ideas = {
				add_idea = modern_society
				remove_idea = outdated_industry
			}
		}
	}
	BAK_scidevdec_one = {

		icon = generic_science

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_res
				has_completed_focus = BAK_ss_res
			}	
		}

		visible = {
			has_idea = lack_of_scientists2
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 365

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_scidevdec_one"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_scidevdec_one"
			swap_ideas = {
				add_idea = lack_of_scientists1
				remove_idea = lack_of_scientists2
			}
		}
	}
	BAK_scidevdec_two = {

		icon = generic_science

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_chrom
				has_completed_focus = BAK_ss_steel
			}	
		}

		visible = {
			has_idea = lack_of_scientists1
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 365

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_scidevdec_two"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_scidevdec_two"
			swap_ideas = {
				add_idea = lack_of_scientists
				remove_idea = lack_of_scientists1
			}
		}
	}
	BAK_scidevdec_three = {

		icon = generic_science

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_marines
				has_completed_focus = BAK_ddcl_marines
			}	
		}

		visible = {
			has_idea = lack_of_scientists
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 365

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_scidevdec_three"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_scidevdec_three"
			remove_ideas = lack_of_scientists
		}
	}
	BAK_scidevdec_four = {

		icon = generic_science

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_fuel
				has_completed_focus = BAK_ddcl_fuel
			}
			NOT = {
				OR = {
					has_idea = lack_of_scientists2
					has_idea = lack_of_scientists1
					has_idea = lack_of_scientists
				}
			}	
		}

		visible = {
			has_idea = poor_science_base
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 547

		modifier = {
			civilian_factory_use = 4
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_scidevdec_four"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_scidevdec_four"
			swap_ideas = {
				add_idea = proper_science_base
				remove_idea = poor_science_base
			}
		}
	}
	BAK_povdevdec_one = {

		icon = generic_political_actions

		available = {
			OR = {
				has_completed_focus = BAK_big_ships
				has_completed_focus = BAK_small_ships
			}	
		}

		visible = {
			has_idea = poverty3
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 730

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_povdevdec_one"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_povdevdec_one"
			swap_ideas = {
				add_idea = poverty2
				remove_idea = poverty3
			}
		}
	}
	BAK_povdevdec_two = {

		icon = generic_political_actions

		available = {
			OR = {
				has_completed_focus = BAK_big_prod_eff_base
				has_completed_focus = BAK_small_prod_eff
			}	
		}

		visible = {
			has_idea = poverty2
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 730

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_povdevdec_two"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_povdevdec_two"
			swap_ideas = {
				add_idea = poverty1
				remove_idea = poverty2
			}
		}
	}
	BAK_povdevdec_three = {

		icon = generic_political_actions

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_chrom
				has_completed_focus = BAK_ss_steel
			}	
		}

		visible = {
			has_idea = poverty1
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 730

		modifier = {
			civilian_factory_use = 1
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_povdevdec_three"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_povdevdec_three"
			swap_ideas = {
				add_idea = poverty
				remove_idea = poverty1
			}
		}
	}
	BAK_povdevdec_four = {

		icon = generic_political_actions

		available = {
			OR = {
				has_completed_focus = BAK_bbbc_infra
				has_completed_focus = BAK_ddcl_infciv
			}	
		}

		visible = {
			has_idea = poverty
			tag = BAK
		}

		cost = 50

		fire_only_once = yes

		days_remove = 730

		modifier = {
			civilian_factory_use = 2
		}

		ai_will_do = {
			factor = 100
		}

		complete_effect = { 
			log = "[GetDateText]: [Root.GetName]: Decision BAK_povdevdec_four"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove BAK_povdevdec_four"
			remove_ideas = poverty
		}
	}
}
BAK_war_category = {
	BAK_bulli_dec = {
		icon = generic_political_actions
        allowed = { tag = BAK }
        visible = {
            OR = {
                has_completed_focus = BAK_big_landair_four
                has_completed_focus = BAK_bbbc_navdoc
                has_completed_focus = BAK_shbb_stab
                has_completed_focus = BAK_bbbc_specops
                has_completed_focus = BAK_bbbc_carrierres
            }
            FROM = {
                exists = yes
                has_war = yes #those are more dynamic so we put them here, since VISIBLE and AVAILABLE are checked more often
            }
        }
        available = {
            FROM = { surrender_progress > 0.7  } #those are more dynamic so we put them here, since VISIBLE and AVAILABLE are checked more often
        }
        target_trigger = {
            FROM = {
                OR = {
                    tag = FBK
					tag = WAT
					tag = FIR
					tag = DMT
					tag = DEP
					tag = RCT
					tag = NIM
					tag = LCT
					tag = WIT
					tag = FRE
					tag = LUS
					tag = GRY
					tag = BAR
					tag = OLV
					tag = GRU
					tag = ZES
                }
                NOT = {
                    OR = {
                        is_in_faction_with = ROOT
                        is_guaranteed_by = ROOT
                        has_non_aggression_pact_with = ROOT
                        is_puppet = yes
                    }
                }
                OR = {
                    ROOT = {
                        is_neighbor_of = FROM
                    }
                    any_country = {
                        is_puppet_of = ROOT
                        is_neighbor_of = FROM
                    }
                }
                NOT = {
                    any_country = {
                        OR = {
                            AND = {
                                is_in_faction_with = ROOT
                                has_war_with = FROM
                            }
                            AND = {
                                is_puppet_of = ROOT
                                has_war_with = FROM
                            }
                        }
                    }
                }
            }
        }
        cost = 0
		war_with_target_on_complete = yes
        ai_will_do = {
            factor = 10
            modifier = {
                factor = 0
                OR = {
                    has_war = yes
                    manpower_per_military_factory < 1000
                    num_divisions < 5
                    has_war_support < 0.5
                    has_stability < 0.65
                    FROM = {
                        alliance_strength_ratio > 0.5
                    }
                    any_country = {
                        has_war_with = FROM
                        alliance_strength_ratio > 0.65
                    }
                }
            }
        }
        complete_effect = {
            FROM = { country_event = { id = bakawar.1 days = 1 } }
        }
	}
	BAK_formfac_dec = {
		icon = generic_political_actions
        allowed = { tag = BAK }
		visible = {
			is_in_faction = no
			has_completed_focus = BAK_small_ships
		}
		available = {
			has_war = no
			OR = {
                has_completed_focus = BAK_small_landair_four
                has_completed_focus = BAK_ddcl_navdoc
                has_completed_focus = BAK_ddcl_warsup
                has_completed_focus = BAK_ddcl_specops
                has_completed_focus = BAK_ddcl_carrierres
            }
		}
		cost = 0
        ai_will_do = {
            factor = 10
        }
        complete_effect = {
            ROOT = { create_faction = BAK_faction }
        }
	}
	BAK_nobulli_dec = {
		icon = generic_political_actions
        allowed = { tag = BAK }
        visible = {
            OR = {
                has_completed_focus = BAK_small_landair_four
                has_completed_focus = BAK_ddcl_navdoc
                has_completed_focus = BAK_ddcl_warsup
                has_completed_focus = BAK_ddcl_specops
                has_completed_focus = BAK_ddcl_carrierres
            }
			ROOT = {
				OR = {
					is_in_faction = no
					AND = {
						is_in_faction = yes
						is_faction_leader = yes
					}
				}
			}
            FROM = {
                exists = yes
				has_defensive_war = yes #those are more dynamic so we put them here, since VISIBLE and AVAILABLE are checked more often
            }
        }
        available = {
            FROM = { surrender_progress > 0.6  } #those are more dynamic so we put them here, since VISIBLE and AVAILABLE are checked more often
        }
        target_trigger = {
            FROM = {
                OR = {
                    tag = FBK
					tag = WAT
					tag = FIR
					tag = DMT
					tag = DEP
					tag = RCT
					tag = NIM
					tag = LCT
					tag = WIT
					tag = FRE
					tag = LUS
					tag = GRY
					tag = BAR
					tag = OLV
					tag = GRU
					tag = ZES
                }
                NOT = {
                    OR = {
                        is_in_faction_with = ROOT
                        is_guaranteed_by = ROOT
                        has_non_aggression_pact_with = ROOT
                        is_puppet = yes
						is_in_faction = yes
                    }
                }
                OR = {
                    ROOT = {
                        is_neighbor_of = FROM
                    }
                    any_country = {
                        is_in_faction_with = ROOT
                        is_neighbor_of = FROM
                    }
                }
                NOT = {
                    any_country = {
                        OR = {
                            AND = {
                                is_in_faction_with = ROOT
                                has_war_with = FROM
                            }
                            AND = {
                                is_puppet_of = ROOT
                                has_war_with = FROM
                            }
                        }
                    }
                }
            }
        }
        cost = 0
        ai_will_do = {
            factor = 10
            modifier = {
                factor = 0
                OR = {
                    has_war = yes
                    manpower_per_military_factory < 1000
                    num_divisions < 5
                    has_war_support < 0.5
                    has_stability < 0.65
                    any_country = {
                        has_war_with = FROM
                        alliance_strength_ratio > 0.65
                    }
                }
            }
        }
        complete_effect = {
            FROM = { country_event = { id = bakawar.2 days = 1 } }
        }
	}
}