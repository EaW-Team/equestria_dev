STG_special_constructions = {
	STG_palace_construction_effort = {

		available = {
			has_completed_focus = the_palace
			has_country_flag = construction_of_the_palace_began
		}

		cost = 40

		days_remove = 3

		ai_will_do = {
			factor = 100
		}
		selectable_mission = yes
		is_good = yes
		days_mission_timeout = 1000
		visible = {
			
		}
		modifier = {
			stability_factor = 0.005
		}
		remove_effect = {
			add_to_variable = { var = STG_soviet_palace_construction value = 2 }
			hidden_effect = {
				activate_mission = STG_palace_construction_effort
			}
		}
		complete_effect = {
			add_stability = 0.05
			set_variable = { var = STG_soviet_palace_construction }
			if = {
				limit = {
					check_variable = { var = STG_soviet_palace_construction value = 7 compare = greater_than }
				}
				add_stability = 0.1
			}
			set_country_flag = palace_construction_continues
		}
	}

}
#STG_something_else = {
#
#	war_thunder = {
#
#	}
#}