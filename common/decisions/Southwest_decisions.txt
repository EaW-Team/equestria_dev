foreign_politics = {
	southwest_war_timer = {

		icon = generic_civil_support

		allowed = {
			OR = {
				original_tag = LSP
				original_tag = SCS
				original_tag = ROC
				original_tag = BUF
			}
		}
		available = {
			always = no
		}

		days_mission_timeout = 300
		#days_remove = 1

		activation = {
			OR = {
				original_tag = LSP
				original_tag = SCS
				original_tag = ROC
				original_tag = BUF
			}
			has_country_flag = southwest_war_countdown_start
		}

		fixed_random_seed = no
		fire_only_once = yes
		is_good = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout southwest_war_timer"
			if = {
				limit = {
					OR = {
						original_tag = SCS
						original_tag = LSP
					}
				}
				declare_war_on = {
					target = BUF
					type = annex_everything
				}
				declare_war_on = {
					target = ROC
					type = annex_everything
				}
			}
			else = {
				custom_effect_tooltip = southwest_war_start_tt
			}
			set_major = yes
		}
	}
}