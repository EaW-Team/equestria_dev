crisis = {
	MAZ_first_warband = {
		icon = generic_ignite_civil_war

		activation = {
			always = no
		}
		available = {
			hidden_trigger = { always = no }
		}
		cancel_trigger = {
			NOT = {
				has_country_flag = crisis_in_progress
			}
		}

		selectable_mission = no
		is_good = no
		days_mission_timeout = 180

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MAZ_first_warband"
			
		}
	}
}
