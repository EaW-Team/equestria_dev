SCS_debug_category = {
	SCS_SW_post_war_debug = {
		
		complete_effect = {
			load_focus_tree = SCS_shareholder_tree
			SCS = {
				annex_country = { target = BUF transfer_troops = no }
			}
			LSP = {
				annex_country = { target = ROC transfer_troops = no}
			}		
		}
	}
}
