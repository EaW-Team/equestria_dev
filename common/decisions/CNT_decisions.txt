political_actions = {
	CNT_search_for_the_cornerstone = {
		icon = generic_nationalism

		allowed = { original_tag = CNT }
		visible = {
			has_idea = CNT_cornerstone_loss
		}
		available = {
			982 = { 
				OR = {
					owns_or_subject_of = yes
					owner = {
						AND = {
							has_opinion = {
								target = ROOT
								value > 74
							}
							NOT = { has_idea = yeti_race }
						}
					}
				}
			}
		}

		cost = 75
		days_remove = 75
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CNT_search_for_the_cornerstone"

			remove_ideas = CNT_cornerstone_loss
		}

		ai_will_do = {
			factor = 100
		}
	}
}
