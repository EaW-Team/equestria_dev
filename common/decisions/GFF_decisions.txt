


# GFF_diplomacy = {
# 	GFF_take_back_cores = {
# 		icon = generic_political_actions
# 		visible = {
# 			281 = { is_owned_by = FROM }
# 			278 = { is_owned_by = FROM }
# 			277 = { is_owned_by = FROM }
# 			FROM = { is_subject_of = ROOT }
# 		}
# 		available = {
# 			has_war = no
# 		}
# 		targets = { YAK }
# 		target_trigger = {
# 			FROM = {
# 				tag = YAK
# 			}
# 		}
# 		fire_only_once = yes
# 		cost = 5
# 		complete_effect = {
# 			log = "[GetDateText]: [Root.GetName]: Decision GFF_take_back_cores target: [From.GetName]"
# 			transfer_state = 281
# 			transfer_state = 278
# 			transfer_state = 277
# 			hidden_effect = {
# 				281 = { add_core_of = ROOT }
# 				278 = { add_core_of = ROOT }
# 				277 = { add_core_of = ROOT }
# 			}
# 		}
# 	}
# }

GFF_power_struggling_cat = {
	GFF_initial_support_commies = {
		icon = generic_political_actions
		visible = {
		}

		available = {
			NOT = {
				has_country_flag = already_doing_a_descision_for_gfff
			}
		}

		cost = 5
		days_re_enable = 45
		days_remove = 20
		
		remove_effect = {
			remove_power_balance_modifier = {
				id = GFF_power_struggling
				modifier = GFF_raise_commies_here
			}
			clr_country_flag = already_doing_a_descision_for_gfff
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GFF_initial_support_commies target: [From.GetName]"
			add_power_balance_modifier = {
				id = GFF_power_struggling
				modifier = GFF_raise_commies_here
			}
			set_country_flag = already_doing_a_descision_for_gfff
		}
	}
	GFF_initial_support_raiders = {
		icon = generic_political_actions
		visible = {
		}

		available = {
			NOT = {
				has_country_flag = already_doing_a_descision_for_gfff
			}
		}

		cost = 5
		days_re_enable = 45
		days_remove = 20
		remove_effect = {
			remove_power_balance_modifier = {
				id = GFF_power_struggling
				modifier = GFF_raise_raiders_here
			}
			clr_country_flag = already_doing_a_descision_for_gfff
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision GFF_initial_support_raiders target: [From.GetName]"
			add_power_balance_modifier = {
				id = GFF_power_struggling
				modifier = GFF_raise_raiders_here
			}
			set_country_flag = already_doing_a_descision_for_gfff
		}
	}
}
