HLQ_diplomacy_category = {

HLQ_seek_imperial_gurantee = {
		icon = griffon
		visible = {
		OR = {
			has_completed_focus = HLQ_the_empire
			has_completed_focus = HLQ_shield_of_griffonkind
			}
			NOT = { is_guaranteed_by = GRI }
			NOT = { is_puppet_of = GRI }
		}
		cost = 50
		days_remove = 35
		remove_effect = {
		GRI = { country_event = { id = hellquill.33 days = 7 } }
		}
		ai_will_do = {
			factor = 100
		}
	}
	
HLQ_bring_colonists = {
		icon = griffon
		visible = {
		OR = {
			has_completed_focus = HLQ_the_empire
			has_completed_focus = HLQ_shield_of_griffonkind
			}
			HLQ = { is_guaranteed_by = GRI }
		}
		cost = 75
		days_remove = 200
		remove_effect = {
		random_owned_controlled_state = {
		limit = { is_core_of = HLQ }
		add_manpower = 12000 
		   }
		random_owned_controlled_state = {
		limit = { is_core_of = HLQ }
		add_manpower = 12000 
		   }
		random_owned_controlled_state = {
		limit = { is_core_of = HLQ }
		add_manpower = 12000 
		   }
		random_owned_controlled_state = {
		limit = { is_core_of = HLQ }
		add_manpower = 12000 
		   }
		}
		ai_will_do = {
			factor = 80
		}
	}

HLQ_invite_bankers = {
		icon = griffon
		visible = {
		OR = {
			has_completed_focus = HLQ_the_empire
			has_completed_focus = HLQ_shield_of_griffonkind
			}
			HLQ = { is_guaranteed_by = GRI }
			NOT = { has_idea = HLQ_bankers }
		}
		cost = 50
		days_remove = 35
		fire_only_once = yes
		remove_effect = {
		add_ideas = HLQ_bankers
		}
		ai_will_do = {
			factor = 80
		}
	}

HLQ_river_gurantee = {
		icon = griffon
		visible = {
			has_completed_focus = HLQ_the_feds
			NOT = { is_guaranteed_by = LCT }
			NOT = { LCT = { has_government = fascism } }
		}
		cost = 50
		days_remove = 35
		remove_effect = {
		LCT = { country_event = { id = hellquill.36 days = 7 } }
		}
		ai_will_do = {
			factor = 100
		}
	}

HLQ_improve_relations = {
		icon = griffon
		visible = {
			has_completed_focus = HLQ_the_feds
			HLQ = { is_guaranteed_by = LCT }
			NOT = { has_country_flag = HLQ_embassies }
		}
		cost = 25
		days_remove = 70
		fire_only_once = yes
		remove_effect = {
		RCT = {
		add_opinion_modifier = {
				target = ROOT modifier = HLQ_diplomacy
			    }
			}
		WIT = {
		add_opinion_modifier = {
				target = ROOT modifier = HLQ_diplomacy
			    }
			}
		NIM = {
		add_opinion_modifier = {
				target = ROOT modifier = HLQ_diplomacy
			    }
			}
		FRE = {
		add_opinion_modifier = {
				target = ROOT modifier = HLQ_diplomacy
			    }
			}
		hidden_effect = {
		set_country_flag = HLQ_embassies
		    }
		}
		ai_will_do = {
			factor = 80
		}
	}

HLQ_honourary_member = {
		icon = griffon
		visible = {
			has_completed_focus = HLQ_the_feds
			HLQ = { is_guaranteed_by = LCT }
			NOT = { has_idea = HLQ_honourary_member }
		}
		cost = 25
		days_remove = 70
		remove_effect = {
		LCT = { country_event = { id = hellquill.39 days = 7 } }
		}
		ai_will_do = {
			factor = 80
		}
	}
}