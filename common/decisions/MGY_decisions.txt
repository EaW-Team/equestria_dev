MGY_somn_decisions_cat = {
	
	MGY_somnambulas_mood = {

		icon = railroad

		available = {
			always = no
		}
		visible = {
			NOT = {
				has_country_flag = MGY_somn_over
			}
		}

		fire_only_once = yes

		cost = 0

		modifier = {
			
		}

		ai_will_do = {
			factor = 10
		}

		}
}

MGY_abyssinia_crisis = {

	MGY_countdown_to_war = {#now it triggers whenever you have 50 WS, and gives you 30 days to declare war. Two Year Proclamation now merely increases War Support gain. Cant happen before the election

		icon = generic_civil_support

		allowed = {
			original_tag = MGY
		}
		available = {#how it is stopped
			OR = {
				AND = {
					owns_state = 741
					owns_state = 764
				}
				has_war_with = ABY
				has_country_flag = MGY_manedalusia_defused
			}
		}

		days_mission_timeout = 30
		#days_remove = 1

		activation = {#what makes it start
				AND = {
					has_country_flag = MGY_war_inevitable
					NOT = {
						has_country_flag = MGY_manedalusia_defused
					}
				}
		}

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MGY_countdown_to_war"
			country_event ={
				id = mgy.35
			}		
		}
	}

}

MGY_ovambo_crisis_category = {

	MGY_ovambo_fast = {

		icon = generic_civil_support

		allowed = {
			original_tag = MGY
		}
		available = {#what causes it to complete
			NOT = {
				has_idea = MGY_ovambo_war
			}		
		}

		days_mission_timeout = 300
		#days_remove = 1

		activation = {#what causes it
			has_idea = MGY_ovambo_war
			NOT = {
				has_government = democratic
				has_country_flag = MGY_ovambo_talks_happened
			}
		}

		fixed_random_seed = no

		is_good = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MGY_countdown_to_war"
			country_event = {
				id = mgy.20
			}		
		}
	}
	MGY_ovambo_slow = {

		icon = generic_civil_support

		allowed = {
			original_tag = MGY
		}
		available = {#if a non-harmony govt makes promises, or a harmony govt ignores them, war comes more slowly
			OR = 
				NOT = {
					has_idea = MGY_ovambo_war				
				}
				NOT = {
					has_government = democratic
				}
			}
		}

		days_mission_timeout = 520
		#days_remove = 1

		activation = {#what will fix it
				OR = {
					has_government = democratic
					has_country_flag = MGY_ovambo_talks_happened
				}
				NOT = {#won't happen if both are true
					AND = {
						has_government = democratic
						has_country_flag = MGY_ovambo_talks_happened						
					}
				}			
		}

		fixed_random_seed = no

		is_good = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MGY_countdown_to_war"
			country_event = {
				id = mgy.20
			}		
		}
	}	

}