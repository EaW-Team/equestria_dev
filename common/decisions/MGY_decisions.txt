MGY_somn_decisions_cat = {
	
	MGY_somnambulas_mood = {

		icon = GFX_decision_generic_political_discourse

		available = {
			always = no
		}
		visible = {
			NOT = {
				has_country_flag = MGY_somn_over
			}
		}

		fire_only_once = yes

		cost = 0

		modifier = {
			
		}

		ai_will_do = {
			factor = 10
		}

		}
}

MGY_abyssinia_crisis_cat = {

MGY_abyssinia_crisis = {

	#now it triggers whenever you have 50 WS, and gives you 30 days to declare war. Two Year Proclamation now merely increases War Support gain. Cant happen before the election

		icon = generic_civil_support

		allowed = {
			original_tag = MGY
		}
		available = {#how it is stopped
			OR = {
				AND = {
					owns_state = 741
					owns_state = 764
				}
				has_war_with = ABY
				has_country_flag = MGY_manedalusia_defused
			}
		}

		days_mission_timeout = 70
		#days_remove = 1

		activation = {#what makes it start
				AND = {
					has_country_flag = MGY_war_inevitable
					NOT = {
						has_country_flag = MGY_manedalusia_defused
					}
				}
		}

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MGY_countdown_to_war"
			country_event ={
				id = mgy.35
			}		
		}
	

}
}

MGY_ovambo_crisis_category = {

	MGY_ovambo_war_happens = {
		icon = generic_civil_support
		allowed = {
			original_tag = MGY
		}
		available = {#What ends it
			NOT = {
				has_idea = MGY_ovambo_war
			}
		}
		days_mission_timeout = 150
		activation = {#What causes it
			has_country_flag = MGY_election_over
		}
		is_good = no
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout MGY_countdown_to_war"
			country_event = {
				id = mgy.20
			}		
		}		
	}
}

MGN_dirty_tricks_cat = {##These decisions could probably be converted into Operations with LaR, once I figure out how to make those work - and get LaR
	MGN_steal_guns = {
		icon = GFX_decision_crossed_swords
		allowed = {
			original_tag = MGN
		}
		days_remove = 7
		days_re_enable = 30		
        targets = { MGY MGC MGF MGH }
		targets_dynamic = yes
        target_trigger = {
            FROM = {
                OR = {
					AND = {
						original_tag = MGY
						NOT = {
							has_government = neutrality
						}
					}
					original_tag = MGH
					original_tag = MGC
					original_tag = MGF
				}
            }
        }
        target_root_trigger = {
			has_government = neutrality
        }
        visible = {
			FROM = {#so we can't steal from defeated factions
                exists = yes
            }
			has_completed_focus = MGN_fight_dirty
        }
        available = {
			has_completed_focus = MGN_fight_dirty
        }
		cost = 25
		remove_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 1000
				} 
			FROM = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -1000
				}
			}
		}
	}
	MGN_start_uprising = {
		icon = GFX_decision_crossed_swords
		allowed = {
			original_tag = MGN
		}
		days_remove = 7		
		days_re_enable = 30
        targets = { MGY MGC MGF MGH }
		targets_dynamic = yes
        target_trigger = {
            FROM = {
                OR = {
					AND = {
						original_tag = MGY
						NOT = {
							has_government = neutrality
						}
					}
					original_tag = MGH
					original_tag = MGC
					original_tag = MGF
				}
            }
        }
        target_root_trigger = {
			has_government = neutrality
        }
        visible = {
			FROM = {#so we can't steal from defeated factions
                exists = yes
            }
			has_completed_focus = MGN_fight_dirty
        }
        available = {
			has_completed_focus = MGN_fight_dirty
        }
		cost = 25
		remove_effect = {
			if = {
				limit = {
					FROM = {
						has_government = democratic
					}
				}
				load_oob = "MGN_cheapshot_harmonists"
			}
			if = {
				limit = {
					FROM = {
						has_government = communism
					}
				}
				load_oob = "MGN_cheapshot_communists"
			}
			if = {
				limit = {
					FROM = {
						has_government = fascism
					}
				}
				load_oob = "MGN_cheapshot_republicans"
			}			
		}
	}
	MGN_dam_river = {
		icon = GFX_decision_crossed_swords
		allowed = {
			original_tag = MGN
		}
		cost = 50
		days_remove = 21
		fire_only_once = yes
		remove_effect = {
			hidden_effect = {
				country_event = { 
					id = mgy.43 days = 91
				}
			}
			if = {
				limit = {
					MGY = {
						OR = {
							has_government = democratic
							has_government = communism
						}
					}
				}
				MGY = {
					add_timed_idea = { idea = MGN_drought days = 90 }				
				}
			}
			if = {
				limit = {
					MGC = { exists = yes }
				}
				MGC = {
					add_timed_idea = { idea = MGN_drought days = 90 }
				}
			}
			if = {
				limit = {
					MGH = { exists = yes }
				}			
				MGH = {
					add_timed_idea = { idea = MGN_drought days = 90 }
				}
			}
		}
	}		
}	