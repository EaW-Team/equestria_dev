ZES_political_spring = {

	ZUG_meeting_timer = {
		allowed = {
			tag = ZUG
		}
		icon = caticon_princess
		activation = {                                                 
			always = no                 
		}
		available = {
			has_country_flag = ZUG_enable_political_game
		}
		days_mission_timeout = 150

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout ZUG_meeting_timer"
			country_event = { id = zugchaos.1 }
			hidden_effect = {
				clr_country_flag = ZUG_enable_political_game
			}
		}
	}

}