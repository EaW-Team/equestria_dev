ED_tree_of_harmony_magic = {
	ED_EQS_friendship_mission = {
		target_root_trigger = {
			has_country_flag = EQS_ED_harmony_magic_enabled
		}

		target_array = EQS_ED_friendship_mission_targets
		targets_dynamic = yes
		
		target_trigger = {
			# For some very stupid reason, targets_dynamic targets ALL dynamics instead of only dynamic tags of targets
			is_in_array = {
				array = EQS_ED_friendship_mission_targets
				value = FROM.original_tag
			}
		}

		fire_only_once = yes
		
		cost = 20 # To be replaced
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ED_EQS_friendship_mission Target: [FROM.GetName]"
			
			set_temp_variable = { target_tag = FROM }
			FROM = {
				EQS_ED_send_friendship_mission_event = yes
			}
			EQS_ED_send_friendship_mission_event = yes
		}
	}
}