DEL_AF_debug_cat = {
	DEL_AF_long_live_the_king = {
		allowed = {
			original_tag = DEL
		}
		available = {
			is_debug = yes
			is_ai = no
		}
		complete_effect = {
			load_focus_tree = DEL_AF_focus
		}
	}
	DEL_AF_add_all_laws = {
		allowed = {
			original_tag = DEL
		}
		available = {
			is_debug = yes
			is_ai = no
		}
		complete_effect = {
			add_ideas = DEL_AF_castanorian_economy
			add_ideas = DEL_AF_undead_waves
			add_ideas = DEL_undead_economy
			add_ideas = DEL_AF_no_science_base
		}
	}
}
