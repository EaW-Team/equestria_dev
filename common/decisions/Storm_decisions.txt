form_storm_kingdom = {
	form_storm_kingdom_decision = {
		icon = crossed_swords
		available = {
			has_war = no
		}
		visible = {
			is_subject = no
			NOT = {
				has_global_flag = SK_formed
			}
			SCT = {
				all_cores_owned_by_ROOT = yes
			}
			STM = {
				all_cores_owned_by_ROOT = yes
			}
			CAM = {
				all_cores_owned_by_ROOT = yes
			}
			CST = {
				all_cores_owned_by_ROOT = yes
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision form_storm_kingdom_decision"
			set_cosmetic_tag = STORM_KINGDOM
			set_capital = 982
			SCT = { add_to_ROOT_potential_cores = yes }
			STM = { add_to_ROOT_potential_cores = yes }
			CAM = { add_to_ROOT_potential_cores = yes }
			CST = { add_to_ROOT_potential_cores = yes }
			hidden_effect = {
				set_global_flag = SK_formed
			}
		}
		ai_will_do = {
			factor = 200
		}
	}
}