EQS_weather_magic_category = {

	EQS_weather_spell_training_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_150_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 150 compare = greater_than_or_equals }
		}
		
		complete_effect = {
			add_to_variable = { weather_pb_var = -150 }
		}
		
		days_re_enable = 30
		days_remove = 30
		
		modifier = {
			experience_gain_army = 0.2
			experience_gain_navy = 0.2
			experience_gain_air = 0.2
			training_time_army_factor = -0.1
			land_mastery_gain_factor = 0.1
			air_mastery_gain_factor = 0.1
			naval_mastery_gain_factor = 0.1
		}
	}
	
	EQS_weather_spell_industrial_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_250_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 250 compare = greater_than_or_equals }
		}
		
		complete_effect = {
			add_to_variable = { weather_pb_var = -250 }
		}
		
		days_re_enable = 30
		days_remove = 30
		
		modifier = {
			experience_gain_army = 0.2
			experience_gain_navy = 0.2
			experience_gain_air = 0.2
			training_time_army_factor = -0.1
			land_mastery_gain_factor = 0.1
			air_mastery_gain_factor = 0.1
			naval_mastery_gain_factor = 0.1
		}
	}
	
	EQS_weather_spell_logistics_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_250_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 250 compare = greater_than_or_equals }
		}
		
		complete_effect = {
			add_to_variable = { weather_pb_var = -250 }
		}
		
		days_re_enable = 30
		days_remove = 30
		
		modifier = {
			special_forces_no_supply_grace = 24
			supply_consumption_factor = -0.1
			supply_node_range = 0.2
		}
	}
	
	EQS_weather_spell_testing_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_350_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 350 compare = greater_than_or_equals }
		}
		
		complete_effect = {
			add_to_variable = { weather_pb_var = -350 }
		}
		
		days_re_enable = 30
		days_remove = 30
		
		modifier = {
			equipment_upgrade_xp_cost = -0.1
			equipment_conversion_speed = 0.1
			production_factory_efficiency_gain_factor = 0.1
			research_speed_factor = 0.1
		}
		
		fixed_random_seed = no 
		
		remove_effect = {
			hidden_effect = {
				random_list = {
					1 = {
						add_breakthrough_points = {  
							specialization = specialization_land
							value = 0.5
						}
					}
					1 = {
						add_breakthrough_points = {  
							specialization = specialization_nuclear
							value = 0.5
						}
					}
					1 = {
						add_breakthrough_points = {  
							specialization = specialization_naval
							value = 0.5
						}
					}
					1 = {
						add_breakthrough_points = {  
							specialization = specialization_air
							value = 0.5
						}
					}
					1 = { 
						add_breakthrough_points = {  
							specialization = specialization_magical
							value = 0.5
						}
					} 
				}
			}
			custom_effect_tooltip = give_50_bp
		}
	} 
	
	EQS_weather_spell_bright_day_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_350_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 350 compare = greater_than_or_equals }
		}
		
		complete_effect = {
			add_to_variable = { weather_pb_var = -350 }
		}
		
		days_re_enable = 45
		days_remove = 45
		
		modifier = {
			air_accidents_factor = -0.1
			air_agility_factor = 0.1
			air_interception_detect_factor = 0.1
			air_cas_present_factor = 0.1
			air_mission_efficiency = 0.1
		}
	}
	   
	EQS_weather_spell_darkness_conditions_target = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		#custom_cost_text = decision_cost_350_wmp
		#custom_cost_trigger = {
		#	check_variable = { var = weather_pb_var value = 350 compare = greater_than_or_equals }
		#}
		 
		complete_effect = { 
		##	add_to_variable = { weather_pb_var = -350 }
			thaumaturgy_roll = yes
		}
		
		state_target = yes 
		
		target_trigger = {
			#FROM.owner = { has_war_with = ROOT }
			FROM = { air_base > 0 } ### We cannot check for airplanes so something at least 
			FROM.owner = { original_tag = STG }
		}
		
		days_re_enable = 45
		days_remove = 45
		
		remove_effect = {
		###	set_temp_variable = { base_chance = 50 }
			
		}	
	}
	
	EQS_weather_spell_attrition_conditions_target = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_350_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 350 compare = greater_than_or_equals }
		}
		complete_effect = {
			add_to_variable = { weather_pb_var = -350 }
		}
		
		target_trigger = {
			has_war_with = ROOT 
		}
		
		target_trigger = {
			FROM.owner = { has_war_with = ROOT }
			divisions_in_state = { 
				size > 0
				state = FROM 
			}
		}
		
		state_target = yes 
		days_re_enable = 45
		days_remove = 45
		
		remove_effect = {
			set_temp_variable = { base_chance = 50 }
			if = {
				limit = {
					thaumaturgy_roll = yes 
				}
				### TODO 
			}
		}
	}
	
	EQS_weather_spell_cloudy_conditions = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_350_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 350 compare = greater_than_or_equals }
		}
	}
	
	EQS_weather_spell_combat_magic_target = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
		custom_cost_text = decision_cost_400_wmp
		custom_cost_trigger = {
			check_variable = { var = weather_pb_var value = 400 compare = greater_than_or_equals }
		}
		complete_effect = {
			add_to_variable = { weather_pb_var = -350 }
		}
		
		target_trigger = {
			FROM.owner = { has_war_with = ROOT }
			divisions_in_state = { 
				size > 0
				state = FROM 
			}
		}
		
		state_target = yes 
		
		days_re_enable = 45
		days_remove = 45
		
		remove_effect = {
			set_temp_variable = { base_chance = 50 }
			if = {
				limit = {
					thaumaturgy_roll = yes 
				}
				### TODO 
			}
		}
	}	
	
#	EQS_weather_spell_naval_suppression = {
	#	visible = { has_completed_focus = EQS_ED_weather_magic }
#		custom_cost_text = decision_cost_250_wmp
#		custom_cost_trigger = {
#			check_variable = { var = weather_pb_var value = 250 compare = greater_than_or_equals }
#		}
#		complete_effect = {
#			add_to_variable = { weather_pb_var = -350 }
#		}
		
#		target_trigger = {
#			has_war_with = ROOT 
#		}
		
#		target_trigger = {
#			is_coastal = yes 
#		}
		
#		state_target = yes 
#		days_re_enable = 45
#		days_remove = 45
		
#		remove_effect = {
#			set_temp_variable = { base_chance = 50 }
#			if = {
#				limit = {
#					thaumaturgy_roll = yes 
#				}
#				### TODO 
#			}
#		}
#	}