ZAR_traditionalism_rising = {
    
    if = {
        limit = {  has_idea = ZAR_traditionalism_favoured }
        swap_ideas = {
            remove_idea = ZAR_traditionalism_favoured
            add_idea = ZAR_traditionalism_dominant
        }
    }

    if = {
        limit = { has_idea = ZAR_tradition_or_progress }
        swap_ideas = {
            remove_idea = ZAR_tradition_or_progress
            add_idea = ZAR_traditionalism_favoured
        }
    }

    if = {
        limit = { has_idea = ZAR_progressivism_favoured }
        swap_ideas = {
            remove_idea = ZAR_progressivism_favoured
            add_idea = ZAR_tradition_or_progress
        }
    }

    if = {
        limit = { has_idea = ZAR_progressivism_dominant }
        swap_ideas = {
            remove_idea = ZAR_progressivism_dominant
            add_idea = ZAR_progressivism_favoured
        }
    }
}

ZAR_progressivism_rising = {

	if = {
		limit = { has_idea = ZAR_progressivism_favoured }
		swap_ideas = {
			remove_idea = ZAR_progressivism_favoured
			add_idea = ZAR_progressivism_dominant
		}
	}	
	if = {
		limit = { has_idea = ZAR_tradition_or_progress }
		swap_ideas = {
			remove_idea = ZAR_tradition_or_progress
			add_idea = ZAR_progressivism_favoured
		}
	}
	if = {
		limit = { has_idea = ZAR_traditionalism_favoured }
		swap_ideas = {
			remove_idea = ZAR_traditionalism_favoured
			add_idea = ZAR_tradition_or_progress
		}
	}
	if = {
		limit = { has_idea = ZAR_traditionalism_dominant }
		swap_ideas = {
			remove_idea = ZAR_traditionalism_dominant
			add_idea = ZAR_traditionalism_favoured
		}
	}
}	
	


ZAR_nomadic_rising = {

    if = {
        limit = {  has_idea = ZAR_nomadic_favoured }
        swap_ideas = {
            remove_idea = ZAR_nomadic_favoured
            add_idea = ZAR_nomadic_dominant
        }
    }

    if = {
        limit = { has_idea = ZAR_nomadic_or_settled }
        swap_ideas = {
            remove_idea = ZAR_nomadic_or_settled
            add_idea = ZAR_nomadic_favoured
        }
    }

    if = {
        limit = { has_idea = ZAR_settled_favoured }
        swap_ideas = {
            remove_idea = ZAR_settled_favoured
            add_idea = ZAR_nomadic_or_settled
        }
    }

    if = {
        limit = { has_idea = ZAR_settled_dominant }
        swap_ideas = {
            remove_idea = ZAR_settled_dominant
            add_idea = ZAR_settled_favoured
        }
    }
}

ZAR_settled_rising = {

	if = {
		limit = { has_idea = ZAR_settled_favoured }
		swap_ideas = {
			remove_idea = ZAR_settled_favoured
			add_idea = ZAR_settled_dominant
		}
	}	
	if = {
		limit = { has_idea = ZAR_nomadic_or_settled }
		swap_ideas = {
			remove_idea = ZAR_nomadic_or_settled
			add_idea = ZAR_settled_favoured
		}
	}
	if = {
		limit = { has_idea = ZAR_nomadic_favoured }
		swap_ideas = {
			remove_idea = ZAR_nomadic_favoured
			add_idea = ZAR_nomadic_or_settled
		}
	}
	if = {
		limit = { has_idea = ZAR_nomadic_dominant }
		swap_ideas = {
			remove_idea = ZAR_nomadic_dominant
			add_idea = ZAR_nomadic_favoured
		}
	}
}	
	

ZAR_martial_rising ={
    if = {
        limit = {  has_idea = ZAR_martial_favoured }
        swap_ideas = {
            remove_idea = ZAR_martial_favoured
            add_idea = ZAR_martial_dominant
        }
    }

    if = {
        limit = { has_idea = ZAR_martial_or_mercantile }
        swap_ideas = {
            remove_idea = ZAR_martial_or_mercantile
            add_idea = ZAR_martial_favoured
        }
    }

    if = {
        limit = { has_idea = ZAR_mercantile_favoured }
        swap_ideas = {
            remove_idea = ZAR_mercantile_favoured
            add_idea = ZAR_martial_or_mercantile
        }
    }

    if = {
        limit = { has_idea = ZAR_mercantile_dominant }
        swap_ideas = {
            remove_idea = ZAR_mercantile_dominant
            add_idea = ZAR_mercantile_favoured
        }
    }
}

ZAR_mercantile_rising = {

	if = {
		limit = { has_idea = ZAR_mercantile_favoured }
		swap_ideas = {
			remove_idea = ZAR_mercantile_favoured
			add_idea = ZAR_mercantile_dominant
		}
	}	
	if = {
		limit = { has_idea = ZAR_martial_or_mercantile }
		swap_ideas = {
			remove_idea = ZAR_martial_or_mercantile
			add_idea = ZAR_mercantile_favoured
		}
	}
	if = {
		limit = { has_idea = ZAR_martial_favoured }
		swap_ideas = {
			remove_idea = ZAR_martial_favoured
			add_idea = ZAR_martial_or_mercantile
		}
	}
	if = {
		limit = { has_idea = ZAR_martial_dominant }
		swap_ideas = {
			remove_idea = ZAR_martial_dominant
			add_idea = ZAR_martial_favoured
		}
	}
}	
	