INS_civil_war_effect = {
	ZOO = { # TODO - decide which TAG will be the Resistance
		division_template = {
			name = "Militia"
			is_locked = yes
			template_counter = 6
			regiments = {
				infantry = { x = 0 y = 0 }
				infantry = { x = 0 y = 1 }
				infantry = { x = 0 y = 2 }
				infantry = { x = 1 y = 0 }
				infantry = { x = 1 y = 1 }
				infantry = { x = 1 y = 2 }
			}
			priority = 0
		}
		for_each_scope_loop = { # Transfers States and gives Cores
			array = IST.controlled_states
			if = {
				limit = {
					IST_initial_uprising_states = yes
				}
				add_to_temp_array = {
					initial_uprising_states_temp_array = THIS
				}
				transfer_state_to = PREV
			}
			if = {
				limit = {
					NOT = { is_core_of = IST }
				}
				add_core_of = PREV
			}
		}
		for_each_scope_loop = { # Creates Divisions
			array = initial_uprising_states_temp_array
			set_temp_variable = { divs_amount_var = resistance }
			divide_temp_variable = { divs_amount_var = 10 }
			round_temp_variable = divs_amount_var
			create_unit = {
				division = "division_template = \"Militia\" start_experience_factor = 0.1"
				owner = PREV
				count = 1 # var:divs_amount_var
				allow_spawning_on_enemy_provs = yes
			}
		}
		declare_war_on = {
			target = IST
			type = civil_war
		}
	}
}