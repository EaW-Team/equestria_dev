#Blame PDX for not allowing the usage of FROM, ROOT and PREV scopes in some effects xd

join_civil_war_LUS= {

	if = {
		limit = { LUS = { has_government = communism has_war_with = D01 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D01 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D02 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D02 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D03 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D03 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D04 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D04 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D05 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D05 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D06 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D06 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D07 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D07 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D08 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D08 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D09 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D09 type = annex_everything }
		break = yes
	}
	if = {
		limit = { LUS = { has_government = communism has_war_with = D10 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "LUS"
				 value = 200
			 }
		}
		add_to_faction = LUS
		declare_war_on = { target = D10 type = annex_everything }
		break = yes
	}
}
################################################################################
################################################################################
################################################################################
################################################################################

join_civil_war_GRU= {

	if = {
		limit = { GRU = { has_government = communism has_war_with = D01 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D01 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D02 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D02 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D03 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D03 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D04 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D04 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D05 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D05 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D06 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D06 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D07 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D07 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D08 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D08 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D09 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D09 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRU = { has_government = communism has_war_with = D10 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRU"
				 value = 200
			 }
		}
		add_to_faction = GRU
		declare_war_on = { target = D10 type = annex_everything }
		break = yes
	}
}

################################################################################
################################################################################
################################################################################
################################################################################

join_civil_war_GRY= {

	if = {
		limit = { GRY = { has_government = communism has_war_with = D01 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D01 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D02 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D02 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D03 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D03 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D04 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D04 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D05 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D05 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D06 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D06 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D07 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D07 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D08 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D08 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D09 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D09 type = annex_everything }
		break = yes
	}
	if = {
		limit = { GRY = { has_government = communism has_war_with = D10 } }
		hidden_effect = {
			 add_ai_strategy = {
				 type = support
				 id = "GRY"
				 value = 200
			 }
		}
		add_to_faction = GRY
		declare_war_on = { target = D10 type = annex_everything }
		break = yes
	}
}