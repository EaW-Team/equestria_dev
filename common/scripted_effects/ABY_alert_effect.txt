#Credit to Fire-Lord-Sozin
#Executed when an alert is left-clicked. Use trigger logic to execute the required effect from the given ABY_alert_id (value in ABY_alert_type_array)
#You also have ABY_alert_index which is the current index of ABY_alert_type_array. Use this to access data from other arrays.
ABY_alert_icon_click_effect = {
	#if = {
	#	limit = { 
	#		check_variable = { ABY_alert_id = 1 }
	#	}
	#	var:ABY_alert_location_array^ABY_alert_index = { PREV = { goto_state = PREV } }
	#}
	#else_if = {
	#	limit = {
	#		check_variable = { ABY_alert_id = 2 }
	#	}
	#}
}
#Will only raise an alert if the country is not an AI and has not hidden that alert type
#You can remove the hidden array check if you don't want the alerts hidden
ABY_alert_raise_1 = {
	if = {
		limit = { is_ai = no NOT = { is_in_array = { ABY_alert_icon_hidden_array = 1 } } }
		#Creates an alert of id 1 with a location in Canterlot
		#The data within an array can be of multiple types (variables, states, countries etc)
		add_to_array = { ABY_alert_type_array = 1 }
		add_to_array = { ABY_alert_location_array = 3.id }
		add_to_array = { ABY_alert_special_array = 0 } #You must add something to all arrays even if you don't use it to preserve index uniformity
		sound_effect = "alert_free_civilian_factories_sound"
	}
}