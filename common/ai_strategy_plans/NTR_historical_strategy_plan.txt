NTR_historical = {
	name = "#NTR - historical (Vedina)"
	desc = ""
	
	enable = {
		tag = NTR
		
		OR = {
			has_country_flag = NTR_AI_VEDINA
			
			has_game_rule = {
				rule = NTR_ai_behavior
				option = VEDINA
			}
			
			AND = {
				is_historical_focus_on = yes
				
				has_game_rule = {
					rule = NTR_ai_behavior
					option = DEFAULT
				}
			}
		}
	}
	
	weight = {
		factor = 1.0
		
		modifier = {
			factor = 1.0
		}
	}
}