can_build_facility_here = { # state scope
	custom_override_tooltip = {
		tooltip = {
			localization_key = CAN_CONSTRUCT_BUILDING_IN_STATE # Root look key
			BUILDING = ANY_FACILITY
			STATE = THIS
		}
		land_facility < 1
		magical_facility < 1
		air_facility < 1
		nuclear_facility < 1
		if = {
			limit = {
				is_coastal = yes
			}
			naval_facility < 1
		}
	}
}