# Scripted triggers for custom alert system
# Full documentation of the system's usage is in common/scripted_guis/eaw_alerts.txt

# Checks if `alert_id` is currently an active alert
has_eaw_alert = {
	is_in_array = { eaw_alerts = alert_id }
}
