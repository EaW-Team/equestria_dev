# This file contain the scripted trigger for VED

VED_is_normal_debug = {
	check_variable = { VED.debug_verbose_level > 0 }
}

VED_is_verbose_debug = {
	check_variable = { VED.debug_verbose_level > 1 }
}