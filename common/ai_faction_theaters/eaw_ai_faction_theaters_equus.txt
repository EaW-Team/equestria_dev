# EQUUS 

olenia = {
	name = theater_olenia
	regions = {
		96 	# Great Mountain Ranges
		98	# Nordbergen
		97 	# Västkusten
		91	# Cervus Dale
		89 # White Range
		88 # Blueberry Lands
		87 # Ostwind 
		113 # Borderlands
		99	# Morkaberg
		90 # Olenia
		93 # Coast of Light
	}
	cancel = {
		OR = {
			AND= {
				# Changelings should focus on EQS
				original_tag = CHN
				has_war_with = EQS
			}
			AND= {
				# Changelings should focus on EQS
				original_tag = CCN
				has_war_with = EQS
			}
			has_war = no 
		}
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 1
			capital_scope = { is_on_continent = europe}
		}
		modifier = {
			add = 5
			OR = {
				has_war_with = CHN
				has_war_with = CCN
				has_war_with = OLE
			}
			OR = {
				original_tag = CHN
				original_tag = CCN
				original_tag = OLE
			}
		}
		modifier = {
			factor = 0
			has_war = no 
		}
	} 
}

northern_front = { # everything from Key Lake to Nova - holy crap these strat regions are massive
	name = theater_northern_front
	regions = {
		114 #Onlartir Mountains
		110	# Key Lake
		109	# Polaria
		108	# Penguinlands
		103 # Greater Yakistan Valley
		115 # North-East Yak Territory
		104 # Modnihendi 
		117 # Lake Kingsmere
		116	# Snowy Mountains
		119 # Beak Region
		107 # Unknown Shore
	}
	cancel = {
		OR = {
			AND= {
				# Changelings should focus on EQS
				original_tag = CHN
				has_war_with = EQS
			}
			AND= {
				# Changelings should focus on EQS
				original_tag = CCN
				has_war_with = EQS
			}
			has_war = no 
		}
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 1
			capital_scope = { is_on_continent = europe}
		}
		modifier = {
			add = 5
			OR = {
				has_war_with = CHN
				has_war_with = CCN
				has_war_with = PLB
				has_war_with = YAK
				has_war_with = JAK
				has_war_with = PNG
				has_war_with = GFF
				has_war_with = GRF
			}
			OR = {
				original_tag = CHN
				original_tag = CCN
				original_tag = PLB
				original_tag = YAK
				original_tag = JAK
				original_tag = PNG
				original_tag = GFF
				original_tag = GRF
				original_tag = STG # because of interest in the region
			}
		}
		modifier = {
			factor = 0
			has_war = no 
		}
	} 
}


alicorn_sunset_north = { # northern front down to crystal ponies
	name = theater_alicorn_sunset_north
	regions = {
		84 # Featherfall Dale
		73 # Yaket Range
		74 # Stonefields
		71 # Crystal Heart
		72 # Crystal Range
		65 # Severyana
		102 # Central Yakistan
		103 # Greater Yakistan Valley
		12 # Frozen North
		191 # Bronze Mountains
		121 # Bright Valleys

	}

	preferred_countries = {
		PLB
	}
	cancel = {
		NOT = { has_war_with = EQS }
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 5
			OR = {
				original_tag = CHN
				is_in_faction_with = CHN
			}
			has_war_with = EQS
		}
		modifier = {
			add = 1
			is_in_faction_with = CHN
			has_war_with = EQS
		}

	} 
}

alicorn_sunset_center = { # straight through to Canterlot
	name = theater_alicorn_sunset_center
	regions = {
		113 # Borderlands
		86 # Flanks
		83 # Whitebell
		75 # Solar Plain
		64 # Oleander Valley
		59 # Ruby Mountains
		61 # Lush Central Plain
		56 # Interfluv
		55 # Midwest Pastures
		41 # Central Equestria
		63 # Neighagra
	}
	preferred_countries = {
		CHN 
	}
	cancel = {
		NOT = { has_war_with = EQS }
	}

	ai_will_do = {
		base = 0 
		modifier = {
			add = 10
			original_tag = EQS
			has_war_with = CHN
		}
	} 
}

alicorn_sunset_south = { # down to Las Pegasus & Buffalo
	name = theater_alicorn_sunset_south
	regions = {
		87 # Ostwind
		53 # Vanhoover
		52 # Coltfoalnia
		51 # Mead Lake
		50 # Las Pegasus
		49 # Aetherlands
		48 # Dodge Junction
		54 # Rocky Region
		36 # Mild West
		39 # Tsoh Halgai
		40 # Hoof Coast
	}
	cancel = {
		NOT = { has_war_with = EQS }
	}
	preferred_countries = {
		OLE BUF
	}
	ai_will_do = {
		base = 0 
		modifier = {
			add = 5
			OR = {
				original_tag = CHN
				is_in_faction_with = CHN
			}
			has_war_with = EQS
		}
	} 
}
