on_actions = {
	on_daily_ROC = {
		effect = {
			if = {
				limit = {
					has_focus_tree = ROC_civil_war_focus
					NOT = {
						has_war_with = LSP
						has_war_with = SCS
						country_exists = SCS
						country_exists = LSP
					}

				}
				country_event = rockunion_postwar.4
			}
		}
	}
}