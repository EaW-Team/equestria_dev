scripted_gui = {
    eaw_HYE_support = { # That's the scripted GUI name, we'll need it later
        window_name = "HYE_support_window_gui"
       #context_type = decision_category
        context_type = player_context # For debugging purpose, it is often good to put it as a player context first

        #dirty = HYE_support_gui_dirty # For later

        visible = {
            always = yes
        }
    }
}