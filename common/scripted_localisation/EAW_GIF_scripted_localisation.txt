# Allows you to create your own dynamic keys
# to be called in localization.
# defined_text -> this is it, we're defining the text
# text -> a discrete entry that can be picked to display in loc.
# trigger -> determines if a text entry will be picked or not.
# (The triggers need to be valid for the scope the key is called in
# (eg Root or From.From).)
# localization_key -> points to the localization key
# that'll be used if trigger passes


defined_text = {
	name = GetPlotPower
	
	text = {
		trigger = {
			check_variable = {
				GIF_plot_power_var > 12
			}
		}
		localization_key = GIF_POWER_VERY_HIGH
	}
	text = {
		trigger = {
			check_variable = {
				GIF_plot_power_var > 6
			}
		}
		localization_key = GIF_POWER_HIGH
	}
	text = {
		trigger = {
			check_variable = {
				GIF_plot_power_var = 6
			}
		}
		localization_key = GIF_POWER_MEDIUM
	}
	text = {
		trigger = {
			check_variable = {
				GIF_plot_power_var < 6
			}
		}
		localization_key = GIF_POWER_LOW
	}
}

defined_text = { # Country
	name = GetGIFReligiousHeadTitle
	text = {
		trigger = {
			GIF = {
					has_country_flag = GIF_war_happened
				}
			}
			localization_key = GetGIFReligiousHeadTitle_abs
		}
	text = {
		trigger = {
			GIF = {
			     NOT = {
					has_country_flag = GIF_war_happened
				}
			}
		}
	    localization_key = GetGIFReligiousHeadTitle_con
   }
}

defined_text = { # Country
	name = GetGIFReligiousHeadDesc
	text = {
		trigger = {
			GIF = {
					has_country_flag = GIF_war_happened
				}
			}
					localization_key = GetGIFReligiousHeadDesc_abs
		}
	text = {
		trigger = {
			GIF = {
			     NOT = {
					has_country_flag = GIF_war_happened
				}
			}
		}
		localization_key = GetGIFReligiousHeadDesc_con
   }
}
